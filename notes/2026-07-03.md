# 2026-07-03

## 今日进展

继续推进 `s-black harness engineering`。

已完成 Stage 4 — 工具适配器层第一版：

- 新增 `docs/06-adapter-layer.md`。
- 新增 `adapters/adapter.schema.json`。
- 新增 `adapters/adapters.sample.json`。

## 关键设计

Adapter 层用于把不同宿主、CLI、Agent runner、外部服务和本地命令包装成统一调用边界。

第一版明确：

- Adapter 只负责边界清晰的执行，不负责绕过授权或直接宣称完成。
- 执行前要走 policy preflight。
- 执行后要走 postflight 并产出 evidence。
- Adapter 输出统一为 `succeeded`、`blocked`、`failed`、`needs_approval`、`needs_input`、`skipped`。
- 工具失败、缺授权、缺输入、网络异常等情况应映射到 task 的 `blocked` 或 `failed`。

第一批样例适配器：

- QwenPaw Agent API
- Kimi Code ACP
- Claude Code ACP
- OMP / pi ACP
- Shell
- 飞书
- GitHub
- WebBridge

## 验证

已验证：

- `adapters/adapter.schema.json` 是合法 JSON。
- `adapters/adapters.sample.json` 是合法 JSON。
- `adapters/adapters.sample.json` 通过 `adapters/adapter.schema.json` 校验。

## Policy 与 Task 衔接

已继续补上 `docs/07-policy-task-bridge.md`。

核心规则：

- `block + deny` -> `blocked / policy_blocked`
- `require_user_approval` -> `blocked / need_user_approval`
- `require_secret_scan` 未完成 -> `blocked`
- completion evidence 不足 -> 不得进入 `finished`
- postflight 失败 -> 回到 `running` 或进入 `blocked`

已新增 `tasks/policy-event.examples.jsonl`，提供 policy 命中、用户授权恢复、secret scan evidence、只读路径阻断、completion evidence 不足等事件样例。

已验证该 JSONL 文件语法有效。

## 最小 CLI 设计

已新增 `docs/08-minimal-cli-design.md`，先设计命令边界，不实现后台服务。

第一版命令：

- `agent-runtime check action`
- `agent-runtime check text`
- `agent-runtime check path`
- `agent-runtime task status`
- `agent-runtime task events`
- `agent-runtime agents list`
- `agent-runtime adapters list`
- `agent-runtime policies list`
- `agent-runtime doctor`

已新增 `cli/commands.sample.json`，作为命令注册表草案，且已验证 JSON 语法有效。

进入真实代码实现时，可以优先委派 Kimi Code 编写 CLI POC；Orchestrator 负责代码审查、公开扫描、JSON/JSONL 校验和行为验收。

## Policy Checker POC 计划

已新增 `docs/09-policy-checker-poc-plan.md`。

该文档明确第一版 Policy Checker 只做只读检查：

- `check text`
- `check path`
- `check action`

同时明确第一版不做：

- 不执行 shell 命令
- 不访问网络
- 不发送消息
- 不删除或写入真实账本
- 不读取 `.env` 或任何密钥文件
- 不持久化用户授权

文档中已写好测试样例、安全要求、验收标准，以及后续委派 Kimi Code 编码时的任务边界。

## CLI POC 实现

已完成最小只读 CLI POC 第一版实现。

新增：

- `agent_runtime/__init__.py`
- `agent_runtime/__main__.py`
- `agent_runtime/cli.py`
- `agent_runtime/doctor.py`
- `agent_runtime/loader.py`
- `agent_runtime/policy.py`
- `agent_runtime/result.py`
- `tests/`

已支持：

- `python -m agent_runtime.cli doctor`
- `python -m agent_runtime.cli check text`
- `python -m agent_runtime.cli check path`
- `python -m agent_runtime.cli check action`
- `python -m agent_runtime.cli agents list`
- `python -m agent_runtime.cli adapters list`
- `python -m agent_runtime.cli policies list`

验收结果：

- `python -m pytest tests -q` -> 23 passed
- `python -m agent_runtime.cli doctor` -> PASS
- public scan -> OK
- GitHub push action -> NEEDS_APPROVAL
- low-risk shell read action -> PASS

安全确认：

- 第一版 CLI 只读。
- 不执行外部命令。
- 不访问网络。
- 不发送消息。
- 不删除文件。
- 不写真实 task ledger。
- 不读取 `.env` 或密钥文件。
- secret scan 不回显完整 match。

## CLI 使用说明与阶段性闭环

已新增 `docs/10-cli-poc-usage.md`，说明当前 CLI POC 的运行方式、命令示例、全局参数、返回码、安全边界和当前限制。

已更新 `README.md` 和 `README.en.md`：

- 当前状态改为只读 CLI POC 已可运行。
- 增加快速开始命令。
- 补齐当前文档列表。

阶段性结论：

本项目已经完成第一阶段闭环：从愿景/路线图，到 policy schema、task state、agent registry、adapter layer、policy-task bridge、CLI 设计、policy checker POC 计划，再到只读 CLI POC 和使用说明。

下一步可以暂缓继续扩展，或在后续单独阶段推进 `check action` 更完整的 publish_rules / completion_rules 关联。
