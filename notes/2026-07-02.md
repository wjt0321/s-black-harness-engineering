# 2026-07-02

## 今日进展

用户确认：Agent Runtime / Harness Orchestrator 作为长期项目推进。

已完成：

- 创建本地项目根目录。
- 建立初始目录结构。
- 写入 README、愿景、路线图、进度账本和项目位置决策。
- 用户指出首版内容是英文，看不懂；这是方向错误。该项目文档默认应使用中文。
- 已将首批文档改为中文。
- 用户要求继续推进，并明确要留下文档供日后参考。
- 已完成第一版通用 Policy Schema 文档：`docs/03-policy-schema.md`。
- 已完成第一版 JSON Schema 草案：`policies/policy.schema.json`。
- 已完成Orchestrator样例 policy：`policies/s-black.sample.policy.json`。
- 已验证两份 JSON 文件语法有效。
- 继续推进 Stage 2 — 任务账本。
- 已完成任务状态模型文档：`docs/04-task-state-model.md`。
- 已完成任务对象 schema：`tasks/task.schema.json`。
- 已完成事件对象 schema：`tasks/event.schema.json`。
- 已完成任务快照样例：`tasks/examples.jsonl`。
- 已完成事件流样例：`tasks/events.examples.jsonl`。
- 已验证 JSON Schema 与 JSONL 样例语法有效。

## 当前判断

现阶段只做框架，不写实际执行器代码，不启动后台服务，不替代 QwenPaw。

Policy Schema 是 Agent Runtime 的规则门禁核心。第一版已经覆盖路径、密钥、危险命令、外发前检查和完成验证。

任务账本第一版决定用 JSONL，不急着上 SQLite；这样更易读、更容易 diff、更适合早期演化。

已继续推进 Stage 3 — Agent 注册表：

- 新增 `docs/05-agent-registry.md`。
- 新增 `agents/agents.schema.json`。
- 新增 `agents/agents.sample.json`。
- 第一版样例包含Orchestrator、Media Agent、Memory Agent、Kimi Code、Claude Code、OMP / pi。
- 已验证两份 Agent 注册表 JSON 文件语法有效。

已按用户要求收完整 Stage 1 — 通用规则模型：

- 新增 `policies/wangcai.sample.policy.json`。
- 新增 `policies/dabai.sample.policy.json`。
- 调整 `policies/policy.schema.json` 的组合规则写法，使三份样例 policy 均可通过 schema 校验。
- 已验证Orchestrator、Media Agent、Memory Agent三份样例 policy 都是合法 JSON，并通过 `policy.schema.json` 校验。
- 已更新 `docs/03-policy-schema.md` 的 Stage 1 落地范围说明。

下一步适合开始设计工具适配器层，或先做 policy 命中后如何进入 task `blocked` 状态的衔接说明。
