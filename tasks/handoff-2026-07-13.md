# Handoff — 2026-07-13 — Stage 14 Closed / Await Next Product Entry

## 当前状态

- 当前分支：`main`，Stage 14 功能收口已推送至 `origin/main`。
- 稳定里程碑：`v0.12.1-orchestration-read-loop-snapshot` / `0419a04`。
- Stage 13 最终收口：`9625ba2`。
- Stage 14 Evidence projection：`4a64ace`。
- Stage 14 最终收口：`03b64dd`。
- 当前无进行中的产品阶段，不创建新 tag，不自动启动 Stage 16。

## Stage 14 已交付

- 七步最小闭环已对账：Task Submit → Routing → Preflight → Dry-run/Controlled Commit → Lifecycle/Event/Artifact/Evidence Projection → Approval → Report。
- read-loop Report Preview 已投影 Evidence candidate 数量与类型，不执行 adapter、不伪造 Evidence。
- `orchestration run inspect --replay` 与 `orchestration report generate --replay` 复用同一 `RuntimeReportResult`，输出 `control-plane/orchestration-replay/v1`。
- `next_action.code` 已冻结为：`proceed_to_commit`、`blocked_wait_for_approval`、`needs_input`、`needs_human_review`、`task_finished`。
- 跨入口一致性、默认兼容、确定性、脱敏、no-write、受控写 rollback、retry/fallback lineage 与 approval 边界均已有测试覆盖。

## 下一轮恢复顺序

1. `python -m agent_runtime.cli docs context --json`
2. `python -m agent_runtime.cli doctor`
3. `docs/000-stage-digest.md`
4. `docs/52-minimal-orchestration-loop.md`
5. `docs/archive/release-notes/77-release-notes-stage14-minimal-orchestration-loop.md`
6. 本 handoff
7. 需要资源/操作边界时再读 `docs/51-backend-first-api-boundary.md`
8. 需要完整路线时再读 `docs/02-roadmap.md`

不要从头遍历 `docs/`，也不要把 `tasks/progress.md` 当作最新状态源。

## 下一步决策点

Stage 14 已收口，下一轮先确认真实消费者和产品入口，再选择后续工作；不要仅因 roadmap 中存在 Stage 16 就直接开始 UI。

需要用户明确的最小决策包括：

1. 消费者是 CLI 自动化、宿主集成、API service，还是 UI / Control Panel；
2. 是否授权引入长期进程、网络协议、鉴权、数据库或真实 adapter execution；
3. 哪些现有 stable / preview read models 需要升级为正式对外契约。

在这些入口未明确前，只允许做缺陷修复、契约一致性维护、文档维护和安全边界回归。

## 不可突破的边界

- 不执行真实 adapter 或外部命令；
- 不访问网络、凭据或外部系统；
- 不引入 UI、service、DB、鉴权、模型代理或长期后台服务；
- snapshot 继续保持 ephemeral read model；
- 受控写入必须显式 `--commit`，并保持写前校验、写后校验和失败回滚；
- 未经用户明确要求，不 push、不创建 tag。

## 提交前验证

```bash
python -m pytest tests -q
python -m agent_runtime.cli doctor
python tools/public_scan.py
python -m pytest tests/test_controlled_write_regression.py -q
python -m compileall -q agent_runtime tests
python -m agent_runtime.cli docs context --json
git diff --check
bash .githooks/pre-commit
```

文档事实源：

- Stage digest：`docs/000-stage-digest.md`
- Stage 14 设计与收口：`docs/52-minimal-orchestration-loop.md`
- Stage 14 验收：`docs/archive/release-notes/77-release-notes-stage14-minimal-orchestration-loop.md`
