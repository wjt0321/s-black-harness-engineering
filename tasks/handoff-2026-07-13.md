# Handoff — 2026-07-13 — Stage 13 Closure / Stage 14 Entry

## 当前基线

- 稳定里程碑：`v0.12.1-orchestration-read-loop-snapshot` / `0419a04`。
- Stage 13 最终验收提交：`9625ba2`。
- 当前阶段：**Stage 14 — 中枢台最小编排闭环**。
- 当前入口：`docs/52-minimal-orchestration-loop.md`。
- Stage 13 事实源：`docs/51-backend-first-api-boundary.md`。
- Stage 13 release notes：`docs/archive/release-notes/76-release-notes-stage13-backend-first-api-boundary.md`。
- 本轮不创建新 tag。

## Stage 13 已完成

- 真实 orchestration CLI surface 已与 51 的资源/操作模型对齐；
- stable、stable（受限）、preview、unavailable 矩阵已冻结；
- 真实命令名与输入边界已修正，尤其是 request_id、task_id、envelope 和 envelope-scoped 查询；
- 新增 `tests/test_orchestration_boundary_contract.py`，冻结命令集合与关键 flag；
- inspect/report/read-loop/snapshot 既有测试覆盖默认输出兼容、preview 字段、确定性和 no-write；
- 协议、鉴权、service、DB、UI、独立持久资源和真实 adapter execution 继续延期。

## 下一轮恢复顺序

1. `python -m agent_runtime.cli docs context --json`
2. `python -m agent_runtime.cli doctor`
3. `docs/000-stage-digest.md`
4. `docs/52-minimal-orchestration-loop.md`
5. `tasks/handoff-2026-07-13.md`
6. 需要边界契约时再读 `docs/51-backend-first-api-boundary.md`
7. 需要 Stage 13 验收事实时再读 `docs/archive/release-notes/76-release-notes-stage13-backend-first-api-boundary.md`

## Stage 14 第一拍

将已冻结的 stable/preview/unavailable 契约应用到最小编排闭环，优先形成可回放、可审计的 use-case 边界：

1. 以 Task intent 作为闭环入口；
2. 复用 routing/preflight，不新增第二套决策逻辑；
3. 区分 dry-run preview、controlled commit 和 blocked/needs_input；
4. 统一 lifecycle event、artifact/evidence/report projection 的输入输出；
5. 验证 replay、next_action、默认输出兼容和 no-write/rollback；
6. 先做本地、受控、无真实 adapter 的闭环，不选择 HTTP/RPC、不启动 service。


## Stage 14 第一拍已完成（本地待提交）

- 已按 52 的七步闭环对照真实 CLI/read models，确认 Submit/Route/Preflight/Run/Approval/Report 已有入口，Step 5 的 Artifact/Evidence 仍是受限投影。
- `OrchestrationReadLoopSnapshot.report` 已补 `evidence_candidate_count` / `evidence_candidate_type_counts`。
- 聚合直接消费 `RunDryRunResult.evidence_candidate_refs`；当前真实 dry-run 默认为空，不执行 adapter、不伪造 evidence。
- 新增结构、未来安全候选类型聚合测试；默认 dry-run 输出不变，snapshot 继续 no-write、deterministic、value-safe。
- 已更新 `docs/50-control-plane-state-model.md` 与 `docs/52-minimal-orchestration-loop.md`。

## 下一步

定义同一 task/request 的最小 replay `next_action` 状态机，并用跨入口一致性测试冻结：

1. 从 task status、lifecycle events、approval decision、envelope/report gate 推导下一动作；
2. 优先复用现有 `task get`、`run inspect`、`report generate` 投影，不新增第二套路由或计划逻辑；
3. 覆盖 proceed-to-commit、wait-for-approval、blocked/needs-input、finished/review 等最小状态；
4. 继续保证默认兼容、脱敏、确定性、no-write；无明确消费者前不建独立 Run/Report collection。

## 安全边界

- 不执行真实 adapter 或外部命令；
- 不访问网络、凭据或外部系统；
- 不引入 UI、service、DB、鉴权或模型代理；
- 受控写入继续显式 `--commit`，并保持写前校验、写后校验和失败回滚。

## 提交前验证

- `python -m pytest tests -q`
- `python -m agent_runtime.cli doctor`
- `python tools/public_scan.py`
- `python -m compileall -q agent_runtime tests`
- `python -m pytest tests/test_controlled_write_regression.py -q`
- task/event ledger validate 与 `task check-ledger`
- `python -m agent_runtime.cli docs context --json`
- `git diff --check`
- `bash .githooks/pre-commit`
