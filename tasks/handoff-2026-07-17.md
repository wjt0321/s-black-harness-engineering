# Handoff — 2026-07-17 — Stage 50 Fixed Execution Operational Recovery Design Gate

> 状态：**Stage 50 design-only gate 已收口**
> 稳定版本：`v0.17.0-filtered-snapshot-display-host-integration`（已推送）
> 当前事实源：`docs/99-fixed-execution-operational-recovery-design-gate.md`
> 下一阶段：Stage 51 — Fixed Execution Operational Recovery Implementation（条件启动）

## 1. 本轮完成

- 审计 Stage 49 trust rotation、`awaiting_terminal`、terminal audit incomplete、Windows Job Object 与 operator workflow；
- 选择 single-flight machine-local lease + safe inspection + outcome-unknown closure + Job accounting 方案；
- 冻结 fixed execution、trust commit/rotation 与 recovery close 共用 exclusive lease；
- 冻结 trust binding `missing/current/drifted/invalid/candidate_unavailable/platform_unavailable` 状态；
- normal rotation 继续要求 reviewed digest/thumbprint、expected old binding id、完整 new executable/PATH identity、`--replace --commit` 与 atomic post-check/rollback；
- lease 固定 atomic create、persistent no-unlink、deny replacement sharing 与 handle/path identity recheck，避免形成两个独立锁域；
- invalid binding 不允许 `--force`、自动删除或静默覆盖；
- 冻结 open attempts 的 16 MiB/50,000 record/64 KiB line/depth-32 bounded list/inspect，`awaiting_terminal` 固定解释为 historical process outcome unknown；
- 冻结唯一 recovery terminal：`execution_failed`、`phase=audit`、`failure_code=execution.recovery_outcome_unknown`、`guard_status=not_run`；
- recovery close 必须绑定 expected started event id 与 expected plan hash，且默认 preview、显式 `--commit`；
- 冻结 Windows Job accounting active-zero、direct-child reap 与 containment-close release gate；
- 历史 `execution-audit/v1` 保持可读，future Job evidence 使用 `execution-audit/v2`；
- 新增 release notes 109，Stage 46 文档移入 archive，活跃 docs 保持 50 个。
- 修复 `bf0b990` 首次 GitHub CI 的跨平台测试夹具问题：模拟 Windows PATH 的测试改用固定 `;`，production code 与 Windows trust contract 不变。
- README/README.en 的首次阅读入口由 30 份历史清单收敛为 7 个当前阶段入口；完整历史继续由 `docs/00-index.md` 导航。

## 2. Production 状态

Stage 50 没有新增 production CLI、schema、writer、subprocess 或真实 operation。当前唯一允许的真实 operation 仍为：

```text
actor = local-operator
adapter_id = shell-local
capability = git_status
operation = git_status
argv = ["git", "status", "--short", "--branch"]
shell = false
```

必须显式使用 `--commit`。CLI 仍不接受 command、argv、cwd、environment、executable path、binding path 或 actor override。

ready 仍只声明：

```text
no_write_contract = true
guard_evidence_passed = true
filesystem_write_proof = false
```

## 3. Stage 51 实现边界

只允许实现 `docs/99-fixed-execution-operational-recovery-design-gate.md` 冻结的：

1. fixed machine-local execution lease；
2. read-only trust inspect；
3. bounded open-attempt list/inspect；
4. fixed outcome-unknown recovery close；
5. Windows Job accounting active-zero evidence；
6. `execution-audit/v1`/v2 validator 与 writer compatibility；
7. CLI contract、doctor、controlled-write regression 与 fake-backend tests。

不得实现：

- automatic retry 或 replay open attempt；
- invalid binding force overwrite；
- POSIX backend；
- linked worktree、submodule 或 alternate object store；
- 第二个 command、任意 argv、shell 或 network adapter；
- service、DB、queue、daemon、background worker 或 UI；
- OS-enforced filesystem write proof。

若 Stage 51 修改 fixed runner，真实 smoke 仍只允许现有 fixed Git status，并需要用户再次明确授权运行。

## 4. 恢复顺序

```bash
python -m agent_runtime.cli docs context --json
python -m agent_runtime.cli doctor
```

然后阅读：

1. `docs/000-stage-digest.md`
2. `docs/99-fixed-execution-operational-recovery-design-gate.md`
3. `docs/98-fixed-git-status-executor-implementation-and-limited-enablement.md`
4. `docs/97-execution-lifecycle-audit-writer-design-and-implementation.md`
5. `docs/archive/96-fixed-git-status-executor-design-gate.md`
6. `docs/archive/95-single-user-real-execution-readiness-gate-and-milestone.md`
7. `docs/archive/release-notes/109-release-notes-stage50-fixed-execution-operational-recovery-design-gate.md`

## 5. Git 边界

- 本轮为 design-only 阶段收口；
- 不创建 tag，稳定 tag 仍为 v0.17.0；
- 不 push；
- 不修改持久 Git 或网络代理配置；
- 不执行真实 subprocess。

## 6. CI correction

GitHub Actions run `29554296000` 在 Ubuntu Python 3.11 上失败于两个 `sanitize_path(..., platform="windows")` 测试。根因是测试使用宿主 `os.pathsep`：Windows 本地得到 `;`，Ubuntu 得到 `:`，与函数显式 Windows contract 不一致。

修复仅调整 `tests/test_execution_trust.py` 的模拟 Windows PATH separator；没有修改 production code、trust policy 或执行权限。定向验证：`python -m pytest tests/test_execution_trust.py -q`，11 passed。
