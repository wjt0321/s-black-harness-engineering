# Handoff — 2026-07-14 — CLI Automation Discovery + Gate + Profile

## 当前状态

- 当前分支：`main`，基于 `origin/main` 的 Stage 14 收口基线继续推进。
- 产品入口：用户明确选择 **CLI 自动化契约深化**。
- 本轮未启动新的产品 Stage，未引入 UI、service、DB、auth、网络或真实 adapter execution。
- 设计事实源：`docs/75-cli-automation-contract-discovery.md`。

## 本轮交付

- 新增 `agent_runtime/orchestration_contract.py`，定义 frozen、确定性的 `control-plane/orchestration-contract/v1`。
- 新增 `orchestration contract inspect` / `--json`。
- manifest 覆盖 `stable`、`stable_limited`、`preview`、`unavailable`，包含 command argv、关键 flag、access 和安全 boundary。
- manifest 自描述 `contract_discovery` 能力；unavailable 条目不伪造命令。
- 新增测试冻结字段、排序、分类计数、determinism、no-write、人类可读输出、argparse command path 与 key flag 一致性。
- 既有 orchestration 命令默认输出未改变。
- 第二拍新增 `orchestration contract check`，复用 manifest 做 requirement negotiation。
- 支持重复 `--require`、`--allow-preview` 与 `--max-access`；输出 `control-plane/orchestration-contract-check/v1`。
- unknown/preview 未授权返回 `needs_input`；unavailable/access 超限返回 `blocked`；逐项结果不丢失。
- manifest 新增 `contract_requirement_gate` 自描述 stable 条目，command path 与 key flags 继续由契约测试校验。
- 第三拍新增 `automation/automation-profiles.schema.json` 与 source-backed sample registry。
- 新增 `orchestration profile list/inspect/check`；check 复用 Requirement Gate，不复制 capability table。
- 首批 profile：`ci-read-only`、`local-dry-run`、`local-controlled-write`。
- doctor 纳入 automation 目录、schema 与 sample validation；manifest 自描述 profile read/check stable 能力。

## 安全边界

- discovery 命令不读取 ledger、adapter registry、环境变量或凭据；
- 不写文件、不访问网络、不执行 manifest 中的命令；
- 不把 preview 升级为 stable；
- 不新增持久 Run/Report collection 或 orchestration Artifact export。

## 验证

<!-- verification-results -->
以下命令均通过：

```bash
python -m pytest tests -q
python -m agent_runtime.cli doctor
python tools/public_scan.py
python -m pytest tests/test_controlled_write_regression.py -q
python -m compileall -q agent_runtime tests
python -m agent_runtime.cli docs context --json
git diff --check
bash .githooks/pre-commit
```

Automation Profile 定向测试覆盖 sample list/inspect/check、stable ordering、unknown profile、missing registry、schema failure、duplicate id、blocked requirement、CLI determinism/no-write 与 human output。
<!-- /verification-results -->

## 下一步建议

下一拍优先定义 read-only Workflow Plan projection：把 Automation Profile 映射为建议的 routing/preflight/run/report 步骤与显式 flag，但只输出计划，不调用 CLI、不写 ledger。

## 恢复顺序

1. `python -m agent_runtime.cli docs context --json`
2. `python -m agent_runtime.cli doctor`
3. `docs/000-stage-digest.md`
4. `docs/75-cli-automation-contract-discovery.md`
5. 本 handoff
6. 需要 Stage 14 闭环事实时读 `docs/52-minimal-orchestration-loop.md`
7. 需要资源/操作矩阵时读 `docs/51-backend-first-api-boundary.md`
