# 2026-07-14 Handoff — Stage 16 Read-only Control Panel MVP Final Closure

## 当前状态

- 当前分支：`main`，仅本地提交，不 push、不创建 tag。
- 当前阶段：**Stage 16 — Read-only Control Panel MVP 已收口**。
- 设计事实源：`docs/76-read-only-control-panel-mvp.md`。
- 验收事实源：`docs/archive/release-notes/79-release-notes-stage16-read-only-control-panel.md`。

## 本轮交付

- 新增 `agent_runtime/orchestration_control_panel.py`。
- 新增 `orchestration control-panel snapshot [--envelope ...]`：输出 `control-plane/control-panel-snapshot/v1` 与 canonical SHA-256 `snapshot_id`。
- 新增 `orchestration control-panel render [--envelope ...]`：向 stdout 输出自包含静态 HTML。
- 固定聚合 overview、tasks、adapters、automation、runs、approvals、artifacts、reports 八个区段。
- overview/tasks/adapters/automation 复用现有项目级 read models；runs/approvals/artifacts 仅在显式 envelope 下投影。
- 未传 envelope 时 scoped 区段显示 `unavailable/envelope_required`；Report 固定显示 request-scoped boundary。
- HTML 采用 industrial audit console 视觉，包含本地过滤、skip link、语义 heading、table caption、focus-visible、reduced-motion、CSP 与字符串 escaping。
- orchestration contract manifest 新增 `control_panel_read` preview/read-only 能力；argparse surface 与 key flag 契约同步冻结。

## 安全边界

- 不启动 HTTP/server/background process。
- 不访问网络，不加载外部字体、脚本、图片或样式。
- 不写文件或 ledger；`render` 只写 stdout。
- 不执行 command 或 adapter。
- 不提供 commit、approval resolve、retry、fallback 或其他写操作按钮。
- 不伪造独立 Run/Report/Artifact collection；scope 边界继续显式。

## 验证

<!-- verification-results -->
收口验证要求：

```bash
python -m pytest tests -q
python -m agent_runtime.cli doctor
python tools/public_scan.py
python -m pytest tests/test_controlled_write_regression.py -q
python -m compileall -q agent_runtime tests
python -m agent_runtime.cli docs context --json
git diff --check
bash .githooks/pre-commit
```

真实 headless Chromium 额外验证：8 个 section 正常渲染、全局过滤从 31 条缩小到 1 条、无 console/page error，并生成 screenshot。
<!-- /verification-results -->

## 明确延期

- live server/API/auth/session；
- DB 与持久 Run/Report/Artifact collection；
- 实时刷新、WebSocket、轮询和在线 availability probe；
- UI controlled write；
- 真实 adapter execution；
- report collection 页面与旧 plan 字段级 diff。

## 下一步

Stage 16 第一拍已经收口，当前不自动启动后续产品阶段。只有出现明确宿主、部署、实时性或 UI 写入需求，并先完成新的设计 gate 后，才继续扩展。

## 恢复顺序

1. `python -m agent_runtime.cli docs context --json`
2. `python -m agent_runtime.cli doctor`
3. `docs/000-stage-digest.md`
4. `docs/76-read-only-control-panel-mvp.md`
5. 本 handoff
6. `docs/archive/release-notes/79-release-notes-stage16-read-only-control-panel.md`
7. 需要 CLI 自动化事实时读 `docs/75-cli-automation-contract-discovery.md`
8. 需要 Stage 14 闭环事实时读 `docs/52-minimal-orchestration-loop.md`
