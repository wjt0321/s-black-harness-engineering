# 2026-07-14 Handoff — Stage 17 已收口 / Stage 18 Design Gate

## 当前状态

- 当前分支：`main`；工作区包含 Stage 17 本地收口提交，未经用户授权不 push。
- 稳定冻结基线：`v0.13.0-read-only-control-plane` / `f401b98`；annotated tag 已推送。
- 当前已完成阶段：**Stage 17 — Control Panel Host Integration Boundary**。
- Stage 17 事实源：`docs/78-control-panel-host-integration-boundary.md`、`docs/archive/release-notes/81-release-notes-stage17-control-panel-host-handoff.md`。
- 当前阶段：**Stage 18 — Read-only Host Consumer Validation**；design gate 已冻结，第一拍采用本地 reference consumer。

## Stage 17 已完成能力

```bash
python -m agent_runtime.cli orchestration control-panel handoff [--envelope PATH] --json
```

- 输出 `control-plane/control-panel-handoff/v1`；
- `snapshot_id` 直接复用现有 snapshot；
- `render_id` 只由 `snapshot_id + control-plane/control-panel-html/v1` 决定；
- descriptor 声明 JSON/HTML media type、encoding、自包含保证、`working_directory=project_root` 与 argv 数组；
- 不传 envelope 时显式列出 `envelope_required` / `request_context_required` scoped boundary；
- project-local 绝对路径归一化为 root-relative 表示，越界路径不回显；
- 不内嵌 HTML、不写文件/ledger、不访问网络、不启动 service、不执行 command/adapter；
- 新命令复用既有 `control_panel_read` contract entry。

## 下一窗口的唯一主任务

按 `docs/79-read-only-host-consumer-validation-boundary.md` 用 TDD 实现第一拍：

1. 新增 `tests/test_control_panel_handoff_consumer.py` 并先确认 RED。
2. 新增标准库-only `tools/control_panel_handoff_consumer.py`。
3. 只从 stdin 读取一份最多 1 MiB 的 UTF-8 JSON；拒绝 duplicate key。
4. 独立校验 schema、handoff/render identity、representation metadata、argv shape 与 boundary。
5. 输出结构化 validation result；不回显原始 descriptor，不执行 argv。
6. 完成全量验证、Stage 18 release notes 与本地提交；不 push、不建 tag。

## 继续延期

- controlled artifact export / `--commit`；
- 自动打开浏览器；
- live server/API/auth/session、DB 与持久 collection；
- WebSocket、轮询、watch mode 与在线 probe；
- UI controlled write、真实 adapter execution；
- 未经设计 gate 的 Codex/QwenPaw 专有耦合。

## 恢复顺序

```bash
python -m agent_runtime.cli docs context --json
python -m agent_runtime.cli doctor
git status --short --branch
```

按顺序阅读：

1. `docs/000-stage-digest.md`
2. `docs/79-read-only-host-consumer-validation-boundary.md`
3. `docs/78-control-panel-host-integration-boundary.md`
4. 本 handoff
5. `docs/archive/release-notes/81-release-notes-stage17-control-panel-host-handoff.md`
6. `docs/77-read-only-control-plane-milestone-freeze.md`
7. `docs/76-read-only-control-panel-mvp.md`
8. 需要 CLI 参数时再读 `docs/10-cli-poc-usage.md`

## 收口验证基线

后续任何代码/schema 改动仍至少运行：

```bash
python -m pytest tests -q
python -m agent_runtime.cli doctor
python tools/public_scan.py
python -m pytest tests/test_controlled_write_regression.py -q
python -m compileall -q agent_runtime tests
python -m agent_runtime.cli docs context --json
git diff --check
bash .githooks/pre-commit
```

未经明确要求，不 push、不创建 tag、不执行真实外部操作。
