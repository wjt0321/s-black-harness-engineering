# 2026-07-14 Handoff — Stage 18 已收口 / Stage 19 Design Gate

## 当前状态

- 当前工作分支：`codex/stage18-reference-consumer`；Stage 18 在隔离 worktree 中完成，未经用户授权不 merge、不 push。
- 稳定冻结基线：`v0.13.0-read-only-control-plane` / `f401b98`；annotated tag 已推送。
- 当前已完成阶段：**Stage 18 — Read-only Host Consumer Validation**。
- Stage 18 事实源：`docs/79-read-only-host-consumer-validation-boundary.md`、`docs/archive/release-notes/82-release-notes-stage18-read-only-host-consumer-validation.md`。
- 下一阶段：**Stage 19 — Host-specific Read-only Adapter Design Gate**；尚未冻结，不直接实现专有宿主 bridge。

## Stage 18 已完成能力

```bash
python -m agent_runtime.cli orchestration control-panel handoff --json | \
  python tools/control_panel_handoff_consumer.py
```

- 新增标准库-only、stdin-only 的 `tools/control_panel_handoff_consumer.py`；
- 输入上限 1 MiB，拒绝空输入、非 UTF-8、非法 JSON、duplicate key；
- 独立校验严格 document shape、schema、producer status、handoff/render identity、representation metadata、argv shape 与安全 boundary；
- project-relative source 同时按 POSIX/Windows 语义校验，拒绝绝对、盘符、根相对与父目录穿越路径；
- 输出 `control-plane/control-panel-host-consumer-validation/v1`，固定 consumer id 与检查顺序；
- `pass`/`blocked`/`validation_failed`/`error` 映射退出码 `0`/`2`/`5`/`1`；
- 不导入 producer、不读取 representation、不执行 argv、不访问网络、不启动 service、不写文件或 ledger。

## 下一窗口的唯一主任务

只做 **Stage 19 design gate**：

1. 在 Codex Desktop、QwenPaw 或其他真实宿主候选中选择一个最小只读接入对象。
2. 明确宿主如何接收 Stage 17 descriptor 与 Stage 18 validation result。
3. 冻结 process lifecycle、刷新策略、错误映射、用户授权与 representation 读取边界。
4. 明确哪些动作保持 unavailable，尤其是 argv execution、文件 export、网络、service 与 UI write。
5. 新增编号设计文档并更新 digest/roadmap/index/handoff；design gate 通过前不改生产代码。

## 继续延期

- descriptor argv 自动执行；
- snapshot/HTML representation 自动读取或自动刷新；
- controlled artifact export / 任意路径写入；
- 自动打开浏览器；
- live server/API/auth/session、DB、WebSocket、polling/watch；
- UI controlled write 与真实 adapter execution；
- 未经设计 gate 的 Codex Desktop/QwenPaw 专有耦合。

## 恢复顺序

```bash
python -m agent_runtime.cli docs context --json
python -m agent_runtime.cli doctor
git status --short --branch
```

按顺序阅读：

1. `docs/000-stage-digest.md`
2. `docs/79-read-only-host-consumer-validation-boundary.md`
3. `docs/archive/release-notes/82-release-notes-stage18-read-only-host-consumer-validation.md`
4. 本 handoff
5. `docs/78-control-panel-host-integration-boundary.md`
6. `docs/archive/release-notes/81-release-notes-stage17-control-panel-host-handoff.md`
7. `docs/archive/77-read-only-control-plane-milestone-freeze.md`
8. 需要 CLI 参数时再读 `docs/10-cli-poc-usage.md`

## 收口验证基线

后续任何代码/schema 改动仍至少运行：

```bash
python -m pytest tests -q
python -m agent_runtime.cli doctor
python tools/public_scan.py
python -m pytest tests/test_controlled_write_regression.py -q
python -m compileall -q agent_runtime tests tools
python -m agent_runtime.cli docs context --json
git diff --check
bash .githooks/pre-commit
```

未经明确要求，不 merge、不 push、不创建 tag、不执行真实外部操作。
