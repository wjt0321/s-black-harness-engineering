# Handoff — 2026-07-23 — Stage 51 Fixed Execution Operational Recovery

> 状态：**Stage 51 implementation 已完成并收口**
> 分支：`feature/stage51-operational-recovery`
> 稳定基线：`v0.17.0-filtered-snapshot-display-host-integration`（已推送）
> 当前事实源：`docs/100-fixed-execution-operational-recovery-implementation.md`
> 下一候选：Stage 52 conditional design gate；无 implementation authority

## 1. 已完成

- shared machine-local execution lease 已覆盖 fixed execution、trust first bind/rotation 与 recovery close；
- trust inspect 已覆盖 missing/current/drifted/invalid/candidate/platform states；
- reviewed rotation 已绑定 expected old binding id 与 exact new executable/PATH identity；
- bounded locked ledger validator 已覆盖 16 MiB、50,000 records、64 KiB line、depth 32 和 identity/strict JSON gates；
- recovery `list-open` / `inspect` / fixed `close-open` 已落地；
- open attempt 固定为 historical outcome unknown，automatic retry/result release 均 false；
- Windows Job accounting 要求 active zero、direct child reaped 与 containment closed；
- historical audit v1 保持可读，新 fixed execution 使用 audit v2。

## 2. CLI surfaces

```text
orchestration execution trust inspect [--json]
orchestration execution recovery list-open [--json]
orchestration execution recovery inspect --attempt-id <attempt-id> [--json]
orchestration execution recovery close-open --attempt-id <attempt-id> --expected-started-event-id <event-id> --expected-plan-hash sha256:<64hex> [--commit] [--json]
```

rotation 的 `--replace --commit` 还要求 `--expected-binding-id`、`--expected-executable-identity` 与 `--expected-path-identity`。

## 3. Verification

production implementation 与本轮文档 closure 后的 full test suite 均通过；不在 handoff 固化测试数量。docs context、doctor、public scan、docs hook 与 diff check 也通过。

Stage 51 没有运行真实 Git status smoke；只使用 fake backend、临时 ledger 与自动化测试。真实项目 ledger 未因 closure 写入。

## 4. 边界

- 不自动 retry；
- 不允许 invalid binding force repair；
- 不开放 POSIX 或第二个 operation；
- 不开放任意 shell/argv、network、service、DB、UI；
- Job accounting 不构成 filesystem write proof；
- 交接创建时未 commit/push/tag/merge；Stage 51 提交后续已在 2026-07-25 按用户授权推送到 `origin/main`，不创建 tag。

## 5. 恢复顺序

1. `docs/000-stage-digest.md`
2. `docs/100-fixed-execution-operational-recovery-implementation.md`
3. `docs/99-fixed-execution-operational-recovery-design-gate.md`
4. `docs/98-fixed-git-status-executor-implementation-and-limited-enablement.md`
5. `docs/97-execution-lifecycle-audit-writer-design-and-implementation.md`
6. `tasks/handoff-2026-07-23.md`
7. release notes 110/109/108/107

## 6. 下一阶段停止线

Stage 52 只能在用户明确启动后做 design gate，候选工作仅是审计 Stage 51 后 remaining risks、operator need 与下一决策。不得把“next stage”解释为实现授权，也不得自动进入 POSIX、第二个 operation、shell/network/service/UI 或 filesystem proof。
