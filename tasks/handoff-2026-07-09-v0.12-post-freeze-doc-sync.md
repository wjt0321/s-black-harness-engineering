# 2026-07-09 v0.12 Post-freeze 文档同步接续上下文

> 本文件供新会话恢复 `s-black harness engineering` 项目使用。当前项目根目录：仓库 checkout 根目录。GitHub 仓库：`https://github.com/wjt0321/s-black-harness-engineering`。

## 当前结论

`v0.12.0-orchestration-foundation` 已经完成真正冻结，不再处于“候选冻结”状态。

当前基线：

- commit：`38b4b69`
- tag：`v0.12.0-orchestration-foundation`
- push：已完成

这意味着：

- `docs/68` / `docs/69` 不再只是“等待执行”的候选材料；
- 后续文档口径必须明确：**v0.12 已冻结完成，当前工作进入 post-freeze 文档整理与下一拍规划**；
- 本轮不要重复做 commit/tag/push 叙事，也不要把 README / roadmap / handoff 写成“还没 freeze”。

## 本轮之前已经完成什么

上一轮已经完成并验证通过：

- `orchestration task submit` A+B
- `orchestration run --retry-of ... --dry-run`
- `orchestration run --fallback-from ... --fallback-to ... --dry-run`
- `docs/63` / `64` / `65` / `66` / `67` / `68` / `69`
- `README.md` / `README.en.md`
- `docs/00-index.md`
- `docs/02-roadmap.md`
- `docs/10-cli-poc-usage.md`
- `tasks/progress.md`

且在冻结前已经通过：

- `python -m pytest tests -q`
- `python -m agent_runtime.cli doctor`
- `python tools/public_scan.py`
- `git diff --check`

## 这轮文档整理的目标

本轮重点不是继续扩功能，也不是重复 freeze 动作，而是把以下口径全部补齐：

1. **基线状态统一**
   - 明确 `v0.12.0-orchestration-foundation` 已是当前稳定基线。
   - 把“候选冻结 / 等待 commit/tag/push”这类旧措辞改成历史语境或已完成语境。

2. **阶段位置统一**
   - Stage 15.97 不再只是“freeze 准备完成”，而应被描述成：
     - 冻结准备材料已齐；
     - 对应基线已在 `38b4b69` 完成冻结与 push；
     - 当前进入 freeze 之后的下一拍文档整理与设计承接。

3. **下一拍方向统一**
   - retry/fallback 目前仍是 dry-run preview；commit 语义还未设计落地。
   - 真实 adapter execution 仍未开放。
   - UI / 服务化 / 数据库仍不是下一拍。
   - 更自然的下一拍，是先把 post-freeze 叙事、阶段收口和后续设计入口整理清楚，再决定是否进入 retry/fallback commit 设计或其他 orchestration backend 设计。

## 当前最重要的三条口径

### 1. v0.12 已是“已完成冻结”的历史基线

不要再写成：

- “尚未执行 commit / tag / push”
- “等待最终 Git 动作”
- “若用户批准再 freeze”

这些表述只应保留在 `docs/69` 这类**执行计划文档的历史语境**里，而不应继续作为 README / roadmap / handoff 的当前状态口径。

### 2. 现在的工作是 post-freeze 文档收口，不是重复发布

本轮的正确目标是：

- 把当前仓库说明改成“基线已完成”；
- 把下一拍入口写清；
- 让新会话读 README / roadmap / handoff 时，不会误判仓库还停在 freeze 前。

### 3. 下一拍更像“设计承接”，不是立刻加写权限

当前最自然的候选方向：

- retry / fallback commit 设计
- run lineage / recovery 事件模型细化
- orchestration backend 文档索引与阶段切分进一步收紧
- read model / controlled write / recovery preview 三者关系再整理

当前不建议直接做：

- 真实 adapter execution
- 网络 / 消息发送接入
- UI / dashboard
- 服务化 / 常驻进程 / 数据库化

## 建议恢复顺序

新会话恢复时，建议按以下顺序读：

1. `README.md`
2. `docs/00-index.md`
3. `docs/02-roadmap.md`
4. `docs/64-versioning-governance.md`
5. `docs/68-orchestration-foundation-milestone-freeze-checklist.md`
6. `docs/69-orchestration-foundation-freeze-execution-plan.md`
7. `tasks/progress.md`
8. `tasks/handoff-2026-07-09-v0.12-post-freeze-doc-sync.md`

如果只读一份恢复全局语境，优先读：

- `tasks/handoff-2026-07-09-v0.12-post-freeze-doc-sync.md`

## 当前推荐下一步

推荐顺序：

1. 先把 README / roadmap / progress / handoff 的“freeze 前口径”全部收成“freeze 已完成”。
2. 再补一份 post-freeze 的阶段说明或 handoff，让下一会话知道当前不再做重复 push。
3. 文档全部整理完成后，再判断是否进入新的 push。
4. push 之后再开启下一拍设计：优先考虑 retry/fallback commit 设计，而不是直接扩执行能力。

## 对下一位维护者的提醒

- 这轮不要再重复做 `v0.12.0-orchestration-foundation` 的冻结动作。
- 如果需要引用冻结证据，直接引用：`38b4b69` + `v0.12.0-orchestration-foundation`。
- `docs/68` / `docs/69` 仍应保留，因为它们记录了冻结前判断与执行方案；但当前状态说明不能继续把它们当“待执行事项”。
- 这轮核心是**文档口径校正**，不是功能扩张。