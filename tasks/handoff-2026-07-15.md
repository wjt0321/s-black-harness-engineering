# 2026-07-15 Handoff — Stage 20 已收口 / Stage 21 Representation Read Design Gate 待启动

## 当前状态

- 当前工作分支：`main`，Stage 20 实现与文档改动已完成，准备创建本地 commit；未经用户授权不 push、不创建 tag。
- 稳定冻结基线：`v0.13.0-read-only-control-plane` / `f401b98`。
- 当前已完成阶段：**Stage 20 — Host-specific Read-only Adapter Implementation**。
- Stage 20 事实源：`docs/81-codex-desktop-read-only-adapter-implementation.md`。
- 下一阶段：**Stage 21 — Read-only Representation Read Design Gate**，尚未启动。

## Stage 20 已完成能力

新增标准库-only、一次性本地 adapter：

```bash
python tools/codex_desktop_read_only_adapter.py \
  --project-root . \
  --timeout-seconds 30 \
  --json
```

- 固定执行 handoff producer 与 Stage 18 reference consumer；
- 输出 `control-plane/codex-desktop-read-only-adapter/v1`；
- `ready` / `blocked` / `validation_failed` / `error` 映射到 `0` / `2` / `5` / `1`；
- 默认每个子进程 30 秒、最大 60 秒；stdout 1 MiB 上限；不自动重试；
- 使用 argv 数组、固定 project root cwd 和最小子进程环境；
- 不执行 descriptor argv，不读取 snapshot/HTML，不写文件/ledger/draft/artifact，不访问网络，不启动服务。

## Stage 21 启动条件

先确认是否存在实际 representation 消费需求，再单独冻结：

1. 用户显式授权动作；
2. snapshot/HTML argv allowlist 和 project-root 路径校验；
3. 内容脱敏、输出上限、超时/取消和 no-write 证明；
4. 与 Stage 18 validation result 的 identity 关联；
5. 是否需要 UI 展示或仍保持 validation-only。

在 Stage 21 design gate 通过前，不扩展当前 adapter，不自动执行 descriptor argv。

## 恢复顺序

```bash
python -m agent_runtime.cli docs context --json
python -m agent_runtime.cli doctor
git status --short --branch
```

按顺序阅读：

1. `docs/000-stage-digest.md`
2. `docs/81-codex-desktop-read-only-adapter-implementation.md`
3. `tasks/handoff-2026-07-15.md`
4. `docs/80-codex-desktop-read-only-adapter-design-gate.md`
5. `docs/79-read-only-host-consumer-validation-boundary.md`
6. `docs/78-control-panel-host-integration-boundary.md`

## 收口验证基线

```bash
python -m pytest tests -q
python -m agent_runtime.cli doctor
python tools/public_scan.py
python -m pytest tests/test_controlled_write_regression.py -q
python -m compileall -q agent_runtime tests tools
python -m agent_runtime.cli docs context --json
git diff --check
bash .githooks/pre-commit
```

Stage 20 本地 smoke command 已通过，当前未 push、未创建 tag。
