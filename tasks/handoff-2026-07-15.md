# 2026-07-15 Handoff — Stage 27 Filtered Snapshot JSON Reader 已收口

## 当前结论

- **Stage 27 — Filtered Envelope Snapshot JSON Reader Implementation 已实现并收口。**
- 既有 `tools/codex_desktop_snapshot_json_reader.py` 已增加 envelope-scoped、结构化 task/request exact filter 的 v3，没有创建平行 reader。
- 支持 task-only、request-only、task+request AND、合法空视图与 request→task 关系闭包。
- v3 使用独立 filtered payload、canonical `filter_id`、Stage 24 base snapshot id 与 canonical `view_id`。
- filter 只作用于完整验证后的 runs/approvals/artifacts 安全 summaries，不进入 handoff/consumer/snapshot child argv。
- 无 filter 的 envelope-scoped 调用保持 Stage 24 v2；无 envelope、无 filter 保持 Stage 22 v1。
- 下一阶段为 **Stage 28 — Filtered Snapshot Host Consumer Validation Gate（条件启动）**。

## 当前基线

- 稳定 milestone：`v0.13.0-read-only-control-plane` / `f401b98`。
- Stage 24 scoped reader 提交：`c71c471`。
- Stage 25 design gate 提交：`5f5df4e`。
- Stage 26 design gate 提交：`cda8a68`。
- Stage 27 为 additive reader 收口，不创建 tag，不改变稳定 milestone。

## 权威入口

1. `docs/000-stage-digest.md`
2. `docs/87-filtered-envelope-snapshot-json-reader-implementation.md`
3. `docs/86-filtered-envelope-snapshot-read-design-gate.md`
4. `docs/84-envelope-scoped-snapshot-read-design-gate.md`
5. `docs/archive/release-notes/89-release-notes-stage27-filtered-envelope-snapshot-json-reader.md`
6. `docs/archive/release-notes/88-release-notes-stage26-filtered-envelope-snapshot-read-design-gate.md`

## Stage 27 CLI

```text
--representation snapshot-json
--envelope <validated-project-relative-path>
[--task-id <task-YYYYMMDD-NNN>] [--request-id <req-YYYYMMDD-NNN>]
```

- filter 至少一个；
- 双参数使用 AND；
- filter 必须与显式 envelope 同时使用；
- 每个 filter flag 最多一次，duplicate 输入结构化失败；
- canonical ASCII exact match，不 trim、不折叠大小写；
- 不接受 list、wildcard、regex、通用 query、sort、page 或 export。

## 匹配与关系闭包

filter 只作用于完整验证后的 runs/approvals/artifacts summaries：

- task-only：先选择 `run.task_id`，再以 selected request ids 包含关联 approval/artifact；
- request-only：三个 section 均按 `request_id` exact match；
- task+request：run 必须同时匹配，再以 selected request set 过滤关联 section；
- 二者不一致或无匹配：返回 `ready`、`matched=false` 的合法空视图。

这使 `adapter_response` 即使安全摘要中 `task_id=""`，仍可通过 request→task 关系闭包进入正确 task view。

## v1 / v2 / v3 兼容

| 输入 | 输出 |
|:---|:---|
| 无 envelope、无 filter | `control-plane/codex-desktop-snapshot-read/v1` |
| envelope、无 filter | `control-plane/codex-desktop-snapshot-read/v2` |
| envelope + task/request filter | `control-plane/codex-desktop-snapshot-read/v3` |
| filter、无 envelope | `validation_failed` / `filter-envelope-required` |
任何显式提供 task/request filter 的请求（包括 filter/envelope 输入门禁失败）均使用 v3 wrapper；未请求 filter 的既有 v1/v2 失败口径不变。


v3 reader id：`codex-desktop-filtered-envelope-snapshot-json-reader/v3`。

## Identity

```text
filter_id = sha256(canonical(filter))
view_id = sha256(canonical(filtered_payload_without_view_id))
```

输出前独立重算并验证：

- canonical filter shape 与 filter id；
- base snapshot id / scope id 形状；
- filtered payload canonical view hash。

representation 分别输出 `base_snapshot_id` 与 `view_id`，不会拿 base snapshot id 冒充 filtered view identity。

## 生命周期与固定执行链

```text
created → scoping → producing → validating → reading → filtering → ready → closed
```

固定 child argv 仍只有：

```text
handoff --envelope <validated-path> --json
reference consumer(stdin=handoff bytes)
snapshot --envelope <validated-path> --json
```

filter 不传给 child process，也不写入 descriptor。

## TDD 与安全边界

Stage 27 先新增失败测试并确认 7 个预期失败，再完成最小实现。覆盖：

- v1/v2 兼容；
- task/request/AND/空视图；
- relation closure；
- invalid/no-envelope/duplicate pre-spawn failure；
- filter/view identity 与 tamper rejection；
- fixed argv 无 filter；
- safe summaries、无 raw values；
- real stdio。

继续禁止：

- descriptor argv 或真实 adapter execution；
- HTML/browser、网络、service、DB、auth/session；
- 缓存、保存、文件 export、UI 写操作；
- arbitrary query、lineage expansion、wildcard/regex、排序/分页；
- raw envelope input、payload refs、raw refs 或 absolute root 回显。

## 文档维护

- 新增 `docs/87-filtered-envelope-snapshot-json-reader-implementation.md`；
- 新增 release notes 89；
- Stage 20 实现文档完整归档至 `docs/archive/81-codex-desktop-read-only-adapter-implementation.md`；
- 活跃文档保持 50 个，归档文档 55 个，全仓 Markdown 约 180 个。

## 下一阶段

**Stage 28 — Filtered Snapshot Host Consumer Validation Gate（条件启动）**。

只有具体宿主需要独立验证 v3 schema、canonical filter、base/filter/view identity 与只读展示边界时启动。不得扩展为 live service、cache/export、HTML/browser 或执行权限。
