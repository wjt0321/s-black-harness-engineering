# Handoff — 2026-07-12 — Stage 12 Closure / Stage 13 Entry

## 当前基线

- 稳定里程碑：`v0.12.1-orchestration-read-loop-snapshot` / `0419a04`。
- Stage 12 最终验收 commit：待本轮首个冻结提交后回填。
- 当前阶段：**Stage 13 — Backend-first API Boundary**。
- 当前设计事实源：`docs/51-backend-first-api-boundary.md`。
- 本轮不创建新 tag；Stage 12 使用 release notes + commit 收口。

## Stage 12 最终结论

Stage 12 已按**确定性、只读、可审计的 control-plane read model**完成验收：

- Task / Event / Run / Approval / Artifact / Evidence / Report 的对象关系与顶层/附属分类已经冻结；
- routing/preflight snapshot、routing snapshot reference 和 Run Preview → candidate Event → Report Preview read loop 已形成可消费投影；
- retry/fallback recovery lineage 已在 `run inspect` 与 `report generate` 两个显式入口复用同一 `control-plane/recovery-lineage/v1` 契约；
- 默认输出兼容、确定性、状态严重度、异常 issue、脱敏与 no-write 边界已有测试覆盖。

最终验收记录：`docs/archive/release-notes/75-release-notes-stage12-control-plane-state-model.md`。

## 正式延期与明确不做

以下事项不是 Stage 12 未完成项，已正式延期：

- 持久化 Run/Event/Report storage；
- snapshot 与持久对象的真实衔接；
- HTTP/RPC、鉴权、长期 service、DB、UI；
- 真实 adapter execution。

Collection-level lineage 当前没有明确消费者，因此不实现 index，也不改造 envelope-scoped `run list`。若未来确有跨 envelope 查询需求，必须先设计独立、一次扫描、只读的 projection。

## 下一轮恢复顺序

1. `python -m agent_runtime.cli docs context --json`
2. `python -m agent_runtime.cli doctor`
3. `docs/000-stage-digest.md`
4. `docs/51-backend-first-api-boundary.md`
5. 本 handoff
6. 需要历史验收事实时再读 `docs/archive/release-notes/75-release-notes-stage12-control-plane-state-model.md`

## Stage 13 第一拍：Boundary Contract Reconciliation

目标是用当前**真实存在**的 CLI/read models 校准 51 文档中的资源与操作契约：

1. 盘点 Task、Run、Approval、Artifact、Report、routing/preflight 等现有入口；
2. 将资源与操作分类为 `stable` / `preview` / `unavailable`；
3. 冻结 identity、status、list/get/action、错误与兼容边界；
4. 标明哪些对象是 ephemeral preview，禁止伪装成持久资源；
5. 形成最小契约文档和失败测试计划，再决定是否进入实现。

本拍不要选择 HTTP/RPC，不启动 service，不引入 auth/UI/DB，也不执行真实 adapter。

## 提交前验证要求

- `python -m pytest tests -q`
- `python -m agent_runtime.cli doctor`
- `python tools/public_scan.py`
- `python -m compileall -q agent_runtime tests`
- `python -m pytest tests/test_controlled_write_regression.py -q`
- task/event ledger validate 与 `task check-ledger`
- `python -m agent_runtime.cli docs context --json`
- `git diff --check`
- `bash .githooks/pre-commit`
