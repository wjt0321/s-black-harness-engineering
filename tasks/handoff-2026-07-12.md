# Handoff — 2026-07-12 — Recovery Read Model Consolidation

## 当前基线

- 冻结里程碑：`v0.12.1-orchestration-read-loop-snapshot` / `0419a04`。
- Recovery aggregation 实现：`6ccd8a9`。
- Report reuse 实现：`7eac7a3`。
- Consolidation 实现：`4f95695`。
- Consolidation 文档收口：`19a546a`。
- 当前分支：`main`；本 handoff 提交完成后应与本文一起推送。

## 本轮完成

- `orchestration run inspect --aggregate-lineage` 与 `orchestration report generate --aggregate-lineage` 共享 `control-plane/recovery-lineage/v1`。
- 新增 `merge_recovery_status()`，统一 inspect/report 的状态严重度矩阵，移除两份重复 precedence。
- 新增 `tests/test_orchestration_recovery_contract.py`，锁定两个入口的 payload、异常 issue、脱敏、确定性和 no-write 一致性。
- branch、missing focus/parent、cross-task parent、cycle、duplicate conflict 均保持相同 aggregation 语义。
- `run list` 仍是 envelope-scoped，不进行逐行 event ledger 聚合。

## 安全与兼容边界

- aggregation 仅在显式 `--aggregate-lineage` 下出现；默认 inspect/report 输出保持兼容。
- 只读取既有 lifecycle events，不扫描 `drafts/`，不写 task/event ledger，不持久化 recovery snapshot。
- 不输出 target、input/payload、raw context、凭据或 envelope 内容。
- 不执行 adapter，不访问网络，不进入 UI、service、DB 或真实 execution。

## 验证证据

在 feature worktree 和 fast-forward 合并后的本地 `main` 上均执行：

- `python -m pytest tests -q`：通过。
- `python -m agent_runtime.cli doctor`：PASS。
- `python tools/public_scan.py`：OK。
- `python -m compileall -q agent_runtime tests`：通过。
- `git diff --check`：通过。
- `.githooks/pre-commit`：docs maintenance check 通过。

## 下一轮恢复顺序

1. `docs/000-stage-digest.md`
2. `docs/74-recovery-lineage-report-reuse.md`
3. `docs/73-recovery-lineage-aggregation-read-model.md`
4. `python -m agent_runtime.cli docs context --json`
5. 本 handoff

## 下一步：Collection-level Lineage Need Assessment

先确认是否存在明确的集合级消费者和查询场景，再决定是否继续：

1. 谁需要跨 envelope 的 lineage 总览：CLI operator、未来 API，还是 report 汇总？
2. 最小查询键是 task、root request、时间范围还是状态？
3. 是否需要独立、只读、一次扫描的 lineage index projection？
4. 如何避免在 `run list` 每一行重复扫描 ledger？
5. 默认输出兼容和异常状态如何保持？

没有明确消费者时，应停在当前 inspect/report 双入口，不新增集合视图。若需要实现，先新增权威设计文档和失败测试，不直接修改 `run list`，也不进入真实执行。
