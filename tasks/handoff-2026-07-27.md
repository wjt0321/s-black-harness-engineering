# Handoff — 阶段 85 已归档，下一里程碑入口已准备

> 日期：2026-07-27
> 状态：阶段 85 已归档；下一里程碑尚未授权启动

## 已完成并归档

阶段 85 已选定“由已经运行的 QwenPaw 宿主被动读取 Runner 注册表，并通过固定临时文件和原子替换发布状态快照”的方案。

归档事实源：

- `docs/archive/134-stage85-external-agent-status-collection-design-review.md`
- `adapters/external-agent-status-collector-design.schema.json`
- `adapters/external-agent-status-collector-design.example.json`
- `tests/test_external_agent_status_collection_design.py`

阶段 84 读取器事实源：

- `docs/archive/133-stage84-bounded-atomic-snapshot-reader-implementation.md`

## 下一里程碑

入口文档：

- `docs/135-next-milestone-real-status-integration.md`

目标是一次性完成下面这条真实可见链路：

```text
OMP/Pi 真实状态
  -> QwenPaw 宿主内状态采集器
  -> 固定原子状态快照
  -> 安全读取器
  -> 中文控制面板
```

下一里程碑必须完成：

1. 核验真实 QwenPaw 宿主状态接口；
2. 实现状态采集器身份绑定和目录访问控制；
3. 实现单写者租约、generation、临时写入、刷盘、原子替换和写后校验；
4. 接通阶段 84 读取器；
5. 在中文控制面板显示真实观察状态和最后更新时间；
6. 完成真实但只读的联调与故障验收。

## 继续保持的停止线

下一里程碑不允许：

- 创建会话；
- 发送提示词或调用模型；
- 派发任务；
- 主动连接 ACP 代替宿主状态接口；
- 读取凭据或访问网络；
- 创建独立后台服务；
- 新增第三个 Harness 真实执行操作；
- 把“观察到 Runner”解释为可以派发。

## 当前验证

阶段 85 收口时已经完成设计专项、中文阶段解析、外部智能体契约回归和全量测试。归档后的核心文档、链接、路径、活跃文档数、doctor、public scan、pre-commit 和 diff check 将在提交前重新验证。

## Git 权限

用户已明确授权本次归档、提交和推送。首次推送使用正常网络；仅在连接阻塞时，对单次 Git 推送临时使用本机 `10808` 代理，不写入永久 Git 代理配置。
