# 2026-07-03 阶段性交接上下文

## 当前结论

`s-black harness engineering` 已完成 v0.1 规划与只读 CLI POC 阶段，并继续完成了若干工程化增量。

当前仓库已经具备：

- 完整的公开 README 中英文入口
- policy / task / event / agent / adapter schema 与样例
- 只读 CLI POC
- 只读 task ledger 查询
- action preflight 聚合检查
- policy profile 选择
- 本地开发包元数据
- GitHub Actions CI
- v0.1 release notes

## 仓库状态

- GitHub: `https://github.com/wjt0321/s-black-harness-engineering`
- 默认分支：`main`
- tag：`v0.1.0-planning-poc`
- 最新已推送 commit：`b21f623 Add CI workflow`
- 当前本地状态：应为 clean

最近关键提交：

```text
b21f623 Add CI workflow
50c3546 Add local package metadata
cf9c805 Add v0.1 release notes
953a51f Add read-only local task ledger examples
cf25d4e Add policy profile selection
3fe1e35 Add read-only task queries and action preflight aggregation
```

## 当前 CLI 能力

可用入口：

```bash
python -m agent_runtime.cli doctor
python -m agent_runtime.cli check text --text hello
python -m agent_runtime.cli check path ./docs/06-adapter-layer.md --read
python -m agent_runtime.cli check action --adapter github-cli --operation git_push --target origin/main
python -m agent_runtime.cli --agent orchestrator check action --adapter shell-local --operation read_file --target docs/README.md
python -m agent_runtime.cli task status task-20260703-001
python -m agent_runtime.cli task events task-20260703-001
python -m agent_runtime.cli task validate --record-file tasks/tasks.jsonl --schema task
python -m agent_runtime.cli task check-ledger --tasks-file tasks/tasks.jsonl --events-file tasks/events.jsonl
python -m agent_runtime.cli agents list
python -m agent_runtime.cli adapters list
python -m agent_runtime.cli policies list
python -m agent_runtime.cli --policy-profile s-black policies
python -m agent_runtime.cli --agent media-agent policies list
```

可选安装入口：

```bash
python -m pip install -e .[dev]
agent-runtime doctor
```

## 已完成增量

### v0.1 planning / POC

- `docs/01-vision-and-boundaries.md`
- `docs/02-roadmap.md`
- `docs/03-policy-schema.md`
- `docs/04-task-state-model.md`
- `docs/05-agent-registry.md`
- `docs/06-adapter-layer.md`
- `docs/07-policy-task-bridge.md`
- `docs/08-minimal-cli-design.md`
- `docs/09-policy-checker-poc-plan.md`
- `docs/10-cli-poc-usage.md`
- `docs/11-release-notes-v0.1.md`

### 只读 CLI POC

- `agent_runtime/cli.py`
- `agent_runtime/doctor.py`
- `agent_runtime/loader.py`
- `agent_runtime/policy.py`
- `agent_runtime/result.py`
- `agent_runtime/tasks.py`
- `tests/`

### 任务账本

- `tasks/task.schema.json`
- `tasks/event.schema.json`
- `tasks/examples.jsonl`
- `tasks/events.examples.jsonl`
- `tasks/policy-event.examples.jsonl`
- `tasks/tasks.jsonl`
- `tasks/events.jsonl`

当前查询策略：

- CLI 优先读取 `tasks/tasks.jsonl` 与 `tasks/events.jsonl`。
- 如果真实 ledger 不存在，才回退 `.examples.jsonl`。
- CLI 仍不写入 ledger。

### Policy profile

`--policy-profile` 支持：

- `s-black`
- `wangcai`
- `dabai`
- `all`

`--policy <file>` 优先于 profile。

### CI

新增：`.github/workflows/ci.yml`

触发：

- push 到 `main`
- pull_request 到 `main`

矩阵：

- Python 3.11
- Python 3.12

执行：

```bash
pip install -e .[dev]
python -m pytest
python -m agent_runtime.cli doctor
python -m agent_runtime.cli task validate --record-file tasks/tasks.jsonl --schema task
python -m agent_runtime.cli task validate --record-file tasks/events.jsonl --schema event
python -m agent_runtime.cli task check-ledger --tasks-file tasks/tasks.jsonl --events-file tasks/events.jsonl
python tools/public_scan.py
```

已确认 GitHub Actions 跑完并成功。

## 当前安全边界

CLI 仍保持只读：

- 不执行外部命令
- 不访问网络
- 不发送消息
- 不删除文件
- 不通过 CLI 写 task ledger
- 不读取 `.env`、`.env.local` 或 credential 文件
- 不回显完整 secret match

公开发布前扫描：

- 已产品化为仓库内脚本 `tools/public_scan.py`。
- 已纳入 CI：`.github/workflows/ci.yml` 在 pytest 与 doctor 后运行 `python tools/public_scan.py`。
- 只读扫描仓库内文本文件，跳过 `.env`、credential 文件与二进制目录。
- 检测 token 模式、Windows 绝对路径、Unix home 路径；命中时只输出相对路径、行号、规则 id，不回显完整命中值。
- 不再依赖仓库外临时脚本。

## 最近验收

本轮已通过：

```text
python -m pytest
84 passed

python -m agent_runtime.cli doctor
PASS
Next: All checks passed.

python tools/public_scan.py
OK public scan
```

GitHub Actions：

```text
completed success Add CI workflow CI main push
```

## 本轮最新进展

已完成 **ledger 写入前 preflight schema 校验**。

新增能力：

```bash
python -m agent_runtime.cli task validate --record-file tasks/tasks.jsonl --schema task
python -m agent_runtime.cli task validate --record-file tasks/events.jsonl --schema event
```

实现内容：

- 新增 `agent_runtime/task_validation.py`。
- 新增 `task validate` CLI 子命令。
- 使用 `tasks/task.schema.json` 与 `tasks/event.schema.json` 校验 JSONL 每行 record。
- 只读取项目根目录内的安全 `.jsonl` 文件。
- 不写入、不修复、不追加 ledger。
- 失败时输出行号、schema 类型和简短错误摘要，不回显整条 record。
- 新增 `tests/test_task_validation.py` 与 `tests/fixtures/`。

已验证：

```text
python -m pytest
66 passed

python -m agent_runtime.cli doctor
PASS

python tools/public_scan.py
OK public scan
```

同时完成 **public scan 产品化**：

- 新增 `tools/public_scan.py`，替代仓库外临时脚本。
- 已加入 CI，在 pytest 与 doctor 之后运行。
- 扫描仓库内文本文件，检测 token 模式与本机路径痕迹；命中只输出相对路径、行号、规则 id。
- 已补充 `tests/test_public_scan.py`。

同时完成 **跨记录 ledger consistency 校验**：

- 新增 `agent_runtime/ledger_consistency.py`。
- 新增 CLI 子命令 `task check-ledger --tasks-file <file> --events-file <file>`。
- 校验 event.task_id 存在性、事件时间顺序、状态流转合法性、终态不可回退、snapshot status 与最新 event to_status 一致。
- 只读 JSONL，不写入 ledger。
- 已补充 `tests/test_ledger_consistency.py`。

同时完成 **agent -> policy profile 自动映射**：

- 新增 `agent_runtime/policy_profile.py`。
- 新增 `--agent <agent-id>` 与 `--assignee <agent-id>` 全局参数。
- 解析优先级：`--policy` > `--policy-profile` > `--agent`/`--assignee` > 默认 `all`。
- 映射规则：orchestrator/s-black -> s-black，media-agent/wangcai -> wangcai，memory-agent/dabai -> dabai，unknown -> all。
- `check text`、`check path`、`check action`、`policies list` 均已接入自动映射。
- 不改变现有 `--policy-profile` 行为，已有测试继续通过。
- 已补充 `tests/test_policy_profile.py`。

## 下一步建议

推荐路线 A：adapter execution envelope 设计。
- 不直接迁移维护者本地脚本。
- 后续再实现仓库内工具并评估是否加入 CI。

## 分工提醒

后续代码实现任务优先交给编码 Agent；主控负责：

- 任务边界说明
- 安全约束
- 代码审查
- 本地验证
- public scan
- 提交与推送
- 对外汇报

## 恢复命令

```bash
git status --short
python -m pytest
python -m agent_runtime.cli doctor
python tools/public_scan.py
python -m agent_runtime.cli task status task-20260703-001
```

如需推送：

1. 先跑测试、doctor、本地 public scan。
2. 直连失败时再临时启用代理。
3. 推送完成后关闭代理并确认系统代理恢复。
