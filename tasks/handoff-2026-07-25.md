# Handoff - 2026-07-25 - Stage 53 Pi Interactive Approval Roundtrip

> 状态：Stage 53 v1 已完成并收口（limited host approval only）
> 稳定基线：`v0.17.0-filtered-snapshot-display-host-integration`（已推送）
> 当前事实源：`docs/102-pi-interactive-approval-roundtrip.md`
> 下一候选：Stage 54 Pi Postflight Audit Design Gate；无 implementation authority

## 1. 已完成

- Stage 52 bridge 保持一次性 stdin/stdout preflight：不执行工具、不写 ledger、不访问网络。
- Stage 53 新增默认关闭的 `AGENT_RUNTIME_APPROVAL_MODE=interactive`。
- v1 仅允许同 cwd 的固定命令 `git push origin main` 请求一次 UI 确认。
- 确认后从当前 event input 重新归一化、重跑 bridge，并精确匹配 request_id、request_hash、tool 与 target_hash。
- 拒绝、超时、无 UI、print/json、输入漂移、bridge 漂移、blocked/invalid 一律阻断。
- 批准不写磁盘、不缓存、不跨 tool call 复用；Harness 不执行 Pi/OMP 工具。
- 批准成功后深冻结 event input，阻止常规后续原地改参。

## 2. 真实 host smoke

本机没有独立 Pi CLI。使用 OMP `17.0.8` 内含的同源 `@earendil-works/pi-coding-agent` ExtensionAPI，以显式 `--extension` 加载 Stage 52 extension：

- `read docs/00-index.md` 放行；
- `read .env` 在执行前阻断；
- `bash git push origin main` 以 needs_approval 在执行前阻断。

没有执行真实 push，没有修改 Pi/OMP 持久配置。

## 3. 验证

- `integrations/pi/test/preflight-bridge.test.ts`：18 项通过。
- Stage 53 后续收口仍需全量 pytest、doctor、public scan、docs context、hook 与 diff check。

## 4. 重要边界

- Pi/OMP API 允许排序更后的 extension 替换整个 `event.input`，宿主没有 final-handler / final-arguments callback 证明；deep freeze 不能把 v1 提升为通用强隔离 approval authority。
- 不应将 v1 与会替换整个 input 的后续 extension 组合使用。
- 不开放 approval ledger、远程批准、跨会话批准、第二个 command、通用 shell/network execution。

## 5. 恢复顺序

1. `docs/000-stage-digest.md`
2. `docs/102-pi-interactive-approval-roundtrip.md`
3. `docs/101-pi-coding-agent-preflight-bridge.md`
4. `docs/100-fixed-execution-operational-recovery-implementation.md`
5. `tasks/handoff-2026-07-25.md`

## 6. 下一阶段停止线

Stage 54 只允许设计 Pi/OMP postflight audit：final arguments identity、脱敏结果投影、失败/取消映射、ledger 边界与 authority wording。未经独立授权不得实现持久 audit writer、扩大批准范围、Harness execution authority 或第二个 command。
