# Handoff — 2026-07-25 — Stage 52 Pi Coding Agent Preflight Bridge

> 状态：**Stage 52 v1 已完成并收口（host-side preflight only）**
> 稳定基线：`v0.17.0-filtered-snapshot-display-host-integration`（已推送）
> 当前事实源：`docs/101-pi-coding-agent-preflight-bridge.md`
> 下一候选：Stage 53 conditional design gate；无 implementation authority

## 1. 已完成

- `agent_runtime/pi_preflight_bridge.py`：一次性 stdin/stdout JSON 预检桥；64 KiB 输入上限、UTF-8、duplicate key、depth、严格最小字段；credential 类目标（.env/.pem 等）直接 blocked；复用 `check_path`/`check_text`/`check_action`。
- CLI：`python -m agent_runtime.cli pi-bridge preflight`（stdin → stdout 单 JSON，始终 JSON；退出码 pass=0 / blocked=2 / needs_approval=3 / invalid=5）。
- 稳定 decision：`pass` / `needs_approval` / `blocked` / `invalid`，附安全 findings、`next_action`、`request_hash` / `target_hash`；不回显 target、命令或文件内容；内部错误 fail closed 为 blocked。
- `adapters/adapters.sample.json` 新增独立条目 `pi-host`（cli_tool / local / 无需 approval；capabilities `tool_call_preflight`、`host_gate`）；不复用 omp-acp。
- `integrations/pi/`：真实可安装 Pi extension（default export `(pi: ExtensionAPI)`，`pi.on("tool_call")` 用 `event.toolName/toolCallId/input`，非 pass 返回 `{block:true, reason}`；`ExtensionAPI` 仅 type import、运行时零 npm 依赖）。官方字段已核对 Pi 当前源码：bash `{command}`、read `{path,offset?,limit?}`、write `{path,content}`、edit `{path,edits:[{oldText,newText}]}`（兼容 legacy 顶层 `oldText/newText`）；`request_id` 来自 `toolCallId`。固定 argv、`shell=false`、timeout 10s（上限 30s）、stdout 64 KiB / stderr 16 KiB、环境白名单、无重试、传输失败本地合成 blocked；`AGENT_RUNTIME_ROOT` 必需，未设置 fail-closed。
- 测试：`tests/test_pi_preflight_bridge.py` 44 项；`integrations/pi/test/preflight-bridge.test.ts` 12 项（真实 spawn Python bridge）。

## 2. CLI surface

```text
printf '%s' '<request-json>' | python -m agent_runtime.cli pi-bridge preflight [--root .]
```

请求契约 `pi-bridge/preflight-request/v1`：read `{path}`；write `{path, content}`；edit `{path, edits: [{old_string, new_string}]}`；bash `{command}`；可选 `request_id`。响应契约 `pi-bridge/preflight-response/v1`。schema 文件在 `integrations/pi/`。

## 3. Verification

专用 44 项 pytest、12 项 node:test、全量 pytest、doctor、public scan、受控写回归与 `git diff --check` 均通过。没有运行任何真实工具执行；没有修改仓库真实 ledger；按用户授权提交并推送到 `origin/main`，不创建 tag。

## 4. 边界

- bridge 只做 host-side preflight enforcement，不是 Harness real adapter execution authority。
- 不执行 read/write/edit/bash；不写 ledger；不访问网络；不启动服务；不修改 Pi 上游。
- 不读取或输出任何凭据；Stage 49 fixed Git status 权限不变。
- 按用户本轮明确授权提交并推送到 `origin/main`；不 tag、不 merge。

## 5. 恢复顺序

1. `docs/000-stage-digest.md`
2. `docs/101-pi-coding-agent-preflight-bridge.md`
3. `docs/100-fixed-execution-operational-recovery-implementation.md`
4. `docs/99-fixed-execution-operational-recovery-design-gate.md`
5. `docs/98-fixed-git-status-executor-implementation-and-limited-enablement.md`
6. `docs/97-execution-lifecycle-audit-writer-design-and-implementation.md`
7. `tasks/handoff-2026-07-25.md`

## 6. 下一阶段停止线

Stage 53 只能在用户明确启动后做 design gate，候选工作仅是审计 Stage 51/52 后 remaining risks、operator need 与下一决策。不得把“next stage”解释为实现授权，也不得自动进入 POSIX、第二个 operation、shell/network/service/UI、bridge execution authority 或 filesystem proof。
