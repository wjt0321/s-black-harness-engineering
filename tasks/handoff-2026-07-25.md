# Handoff - 2026-07-25 - Stage 61 Pi Controlled Dry-run Adapter Contract（design gate）

> 状态：Stage 61 design gate 已冻结（design-only；未授权实现）
> 稳定基线：`v0.17.0-filtered-snapshot-display-host-integration`（已推送）
> 当前事实源：`docs/110-pi-controlled-dry-run-adapter-contract.md`
> 下一候选：Stage 62 Pi Controlled Dry-run Print Implementation（条件启动，需用户再次授权真实模型调用与第二个真实 operation）；真实终端人工 TUI 会话验收（operator 执行）

## 1. 已完成

- Stage 52 bridge 保持一次性 stdin/stdout preflight：不执行工具、不写 ledger、不访问网络。
- Stage 53 `AGENT_RUNTIME_APPROVAL_MODE=interactive` 仍默认关闭，有限支持同 cwd 的固定 `git push origin main` 一次确认。
- Stage 54 新增默认关闭的 `AGENT_RUNTIME_POSTFLIGHT_MODE=summary`。
- 启用后，extension 监听 Pi/OMP `tool_result`，使用当前 `event.input` 重跑 Stage 52 preflight，并向结果 content 追加一个 value-free 摘要块。
- 摘要包含 tool、decision、request/target hash、hashed toolCallId、content block count、text chars 与原始 `isError`；不包含 path、command、tool output text、details 或 credential-like values。
- Stage 54 不写 Harness ledger、不访问网络、不改 `isError`、不 patch details，也不声明 Harness 执行了工具。

## 2. 真实 host smoke

本机没有独立 Pi CLI。Stage 52 已使用 OMP `17.0.8` 内含的同源 `@earendil-works/pi-coding-agent` ExtensionAPI，以显式 `--extension` 加载 extension 验证：

- `read docs/00-index.md` 放行；
- `read .env` 在执行前阻断；
- `bash git push origin main` 以 needs_approval 在执行前阻断。

Stage 54 尚未做持久安装 smoke；没有执行真实 push，没有修改 Pi/OMP 持久配置。

## 3. 验证

- `integrations/pi/test/preflight-bridge.test.ts`：22 项通过。
- 覆盖 default-off postflight、pass summary、blocked summary、敏感 target 不回显、tool output 不进入 summary、`isError` 不变、ungated/malformed result 不变。
- 后续收口仍需全量 pytest、doctor、public scan、docs context、hook 与 diff check。

## 4. 重要边界

- Stage 54 是 host-side projection，不是 durable audit writer。
- `tool_result.input` 来自 Pi/OMP 当前事件；它是最终参数投影来源，但不是 Harness execution authority。
- Projection 保留原始 content 并追加摘要；它不是 output redaction layer。
- 不开放 approval ledger、远程批准、跨会话批准、第二个 command、通用 shell/network execution 或持久 postflight writer。

## 5. 恢复顺序

1. `docs/000-stage-digest.md`
2. `docs/103-pi-postflight-audit-projection.md`
3. `docs/102-pi-interactive-approval-roundtrip.md`
4. `docs/101-pi-coding-agent-preflight-bridge.md`
5. `docs/100-fixed-execution-operational-recovery-implementation.md`
6. `tasks/handoff-2026-07-25.md`

## 6. Stage 57 完成与 Stage 58 停止线

- Stage 57 已获一次受控模型调用授权：隔离目录、仅内置 `read`，不启用 approval/postflight，不执行真实 push 或写操作。
- 初始阻塞点已复盘：Pi 内置 DeepSeek / CLI print/json mode 出现 0 stdout / 0 stderr 超时；Node 直连 DeepSeek API 正常。
- 已写入可回滚 `%USERPROFILE%\.pi\agent\models.json` 自定义 provider `deepseek-compat`，通过 `$DEEPSEEK_API_KEY` 解析密钥，不写明文；写入前备份位于 `%USERPROFILE%\.pi\agent\backups\stage57-before-models-json-20260725-175734`。
- SDK 直连 `createAgentSession` 成功加载全局 extensions（含 `pi-preflight-bridge`），模型发起一次 `read stage57-proof.txt`，工具执行完成，最终文本为 `STAGE57_OK:PI_LAYER1_REAL_SESSION_OK`。
- 完整事件证据位于 `%USERPROFILE%\.pi\agent\backups\stage57-sdk-real-read\sdk-real-read-events.json`。

## 7. Stage 58 项目本地运行时整合

- 用户要求将 Pi 直接整合进 `<project-root>`，便于统一管理。
- 当前用户级 `PI_CODING_AGENT_DIR=<project-root>\.runtime\pi-agent`。
- `.runtime/` 已加入 `.gitignore`；auth、session、backup 与模型运行配置不会进入 Git。
- 原 `%USERPROFILE%\.pi\agent` 未删除，保留作回滚副本。
- 新目录已通过模型发现和 extension 自动发现 smoke：普通 read 放行、`.env` 阻断、固定 push needs_approval。
- 下一候选只处理 Pi CLI `--print` / `--mode json` / TUI 稳定性；未经新授权，不开放 bash/write/edit、approval/postflight、真实 push 或 Harness ledger。

## 8. Stage 59 CLI/TUI mode 稳定门

- 0 输出超时在当前组合下不再现（7+ 次受控运行 2–6s，rc=0）；根因：内置 deepseek provider API 侧不稳定 + 默认模型解析漂移。
- 修复仅两处：gitignored `.runtime/pi-agent/settings.json` 钉住 `deepseek-compat/deepseek-v4-flash`（有备份）；提交 `integrations/pi/smoke/cli-mode-smoke.sh`（首跑 5/5 PASS）。
- 入口契约与「smoke 会发起真实模型调用」披露见 `docs/108` 第 3 节；TUI 必须在真实终端运行，非 TTY fail-fast。
- smoke 命令：`bash integrations/pi/smoke/cli-mode-smoke.sh`（需 `DEEPSEEK_API_KEY`，永不打印）。
- 未修改 Pi 上游、extension 代码、approval/postflight 开关；未 commit/push。

## 9. docs 归档收敛（2026-07-25 已执行）

用户批准后已执行：`git mv` 将以下 12 份已冻结/被取代的设计文档从 `docs/` 完整移入 `docs/archive/`，内容未删除，历史由 Git 保留：

`41-runtime-event-import-consistency-freeze`、`45-runtime-event-import-strict-freeze-mode`、`54-backend-preparation-before-ui`、`56-orchestration-controlled-write-boundary`、`58-orchestration-run-controlled-execution-design`、`60-orchestration-run-lifecycle-events-design`、`62-orchestration-task-submit-controlled-write-design`、`63-orchestration-task-submit-created-event-design`、`66-orchestration-run-retry-fallback-design`、`70-orchestration-run-retry-fallback-commit-design`、`73-recovery-lineage-aggregation-read-model`、`74-recovery-lineage-report-reuse`

执行结果：

- docs/ 活跃文档 59 → **47**（≤50 达标）；`docs/archive/` 88 → 100。
- 活跃区全部引用已改为 `archive/` 或 `docs/archive/` 路径：`00-index`、`000-stage-digest`、`02-roadmap`、`10-cli-poc-usage`、`64`、`76`、`86`、`97`、`98`、archive/68/69/95/96、release-notes 42/44/46/57/59/61/65/67/71/72/75、`tasks/handoff-2026-07-07-*`、`tasks/handoff-2026-07-12`、`tasks/progress.md`，以及被移动文件之间的相互引用。
- 引用修复后全仓 grep 确认：活跃区不再存在指向旧根路径的引用。
- 未触碰：`.worktrees/` 历史副本（保留旧引用，属历史快照）；`docs/MAINTENANCE.md` 中 58/66 仅为格式示例，按用户指示保留。
- 恢复上下文保留：digest 摘要、release notes 与 archive 内文档均未删除；`00-index` 第 5 节与 digest 恢复顺序仍连贯。

## 10. Stage 60 Pi adapter discovery 与 capability/readiness 投影

- registry 末尾追加 `pi-cli`（kind `pi_cli → agent`）；capabilities 全新唯一，既有路由逐字节不变；`risk_level=external` 保持 needs_approval，无执行权限。
- 新增 `agent_runtime/pi_runtime_discovery.py`：7 项确定性 fail-closed 检查；64 KiB 有界读取；绝不读取 auth/session/.env/凭据；apiKey 仅接受 `$ENV_VAR` 引用且值不回显。
- 接入：`orchestration adapter inspect pi-cli` 附加 `local_runtime`；`orchestration preflight` 未就绪 blocked / 就绪 needs_approval；control-panel snapshot 自动包含（adapter 9→10）。
- 测试：`tests/test_pi_runtime_discovery.py` 19 项；全量 pytest 1390 passed / 0 failed（8 个 skip 为既有）；doctor / public scan / diff check 通过。
- 限制：无法探测 Pi 版本（禁进程执行）；readiness 依赖调用时 env；CI/无 env 下 pi-cli preflight 一律 fail closed（设计行为）。
- 未 commit/push；`.runtime/` 未进入 Git；extension 未改。


## 11. Stage 61 Pi controlled dry-run adapter contract（design-only）

- 新增 `docs/110-pi-controlled-dry-run-adapter-contract.md`：冻结唯一候选 operation `pi_cli_print` 的受控 dry-run 契约（固定 argv `pi --print --no-session --no-tools`，prompt 唯一可控输入且 4 KiB 有界、secret scan 前置）。
- 关键决策：`--no-session`/`--no-tools` 使本地副作用面最小（无 session 写入、无工具执行）；环境显式 allowlist 重建，`DEEPSEEK_API_KEY` 只经 env 透传且永不回显；诚实声明 npm 安装完整性信任缺口，v1 不宣传为 trusted executable chain；复用 Stage 49 Windows Job Object runner 语义与 Stage 47–48 audit 链；模型回答文本不进入 Harness 公开投影。
- read 工具 roundtrip、`--mode json`、canonical approval binding、POSIX 均为独立后续候选；推荐下一实现为 Stage 62（条件启动，需用户再次授权真实模型调用与第二个真实 operation）。
- 本 stage 无代码、无 CLI、无 subprocess、无新增测试；docs context / public scan / diff check / pre-commit hook 通过；未 commit/push。
