# Handoff — 2026-07-16 — Stage 35 Filtered Snapshot Display Integration Milestone Freeze

> 状态：**Stage 33–35 已收口；本地 annotated tag 随本次收口创建，不 push**
> 当前里程碑：`v0.15.0-filtered-snapshot-display-integration`
> 下一阶段：Stage 36 — Filtered Snapshot Markdown Display Consumer Validation Gate（条件启动）

## 1. 本轮完成

- Stage 33：冻结 fixed Stage 31 host → strict gate → escaped deterministic Markdown design；
- Stage 34：按 TDD 实现 `tools/codex_desktop_filtered_snapshot_display.py`；
- Stage 35：冻结 `v0.15.0-filtered-snapshot-display-integration` 本地里程碑。

权威事实源：

- `docs/91-codex-desktop-filtered-snapshot-markdown-display-integration-and-milestone-freeze.md`
- `docs/archive/release-notes/94-release-notes-stage33-filtered-snapshot-display-integration-gate.md`
- `docs/archive/release-notes/95-release-notes-stage34-codex-desktop-filtered-snapshot-markdown-display.md`
- `docs/archive/release-notes/96-release-notes-v0.15.0-filtered-snapshot-display-integration.md`

## 2. 新增实现

```text
tools/codex_desktop_filtered_snapshot_display.py
tests/test_codex_desktop_filtered_snapshot_display.py
```

固定 contract：

```text
control-plane/codex-desktop-filtered-snapshot-display/v1
codex-desktop-filtered-snapshot-markdown-display/v1
```

固定调用：

```bash
python tools/codex_desktop_filtered_snapshot_display.py \
  --project-root . \
  --envelope adapters/execution-envelope.examples.json \
  --request-id req-20260703-001 \
  --representation markdown \
  --json
```

## 3. 冻结行为

- 只启动固定 Stage 31 host，stdin none、minimal env、shell false；
- ready 前验证 host schema/id/source/lifecycle/guarantees/status-exit、identity、safe rows、counts/matched 与 filter semantics；
- 动态值只以 escaped ASCII JSON inline literal 进入固定 Markdown；
- request-only、task-only、AND 与合法空视图均支持；
- content id 为 Markdown UTF-8 bytes SHA-256；最终 JSON 64 KiB；
- host 非 ready 或任何 drift 时 content withheld/fail closed；
- no direct reader/consumer、no retry、no write/network/service/cache/export/execution。

## 4. 验证证据

- RED：11 项测试因工具不存在而预期失败；
- GREEN：11 项专用测试通过；
- 78 项相关 reader/consumer/host/display 回归通过；
- 868 项全量测试通过；
- doctor PASS；public scan OK；py_compile PASS；
- request-only、task-only、AND、empty-view 真实 smoke ready/0；missing envelope blocked/2 且 withheld。

## 5. Stage 36 条件启动边界

第一拍只允许设计独立 stdin consumer，验证 display v1 wrapper、content/content-id、escaping invariant、empty-view 与 non-ready withheld。不得：

- 启动 display/host/reader 或执行 descriptor/candidate/adapter；
- 接受 file/URL/payload-only/arbitrary Markdown；
- 引入专有 UI、HTML/browser、service/network/DB/auth；
- 持久化、cache/export、clipboard/file write 或 UI controlled write。

## 6. Git 停止线

- 创建本地 commit；
- 创建本地 annotated tag `v0.15.0-filtered-snapshot-display-integration`；
- 本轮用户未明确要求 push，因此不 push，等待后续指挥。
