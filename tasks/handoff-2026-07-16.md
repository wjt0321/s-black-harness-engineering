# Handoff — 2026-07-16 — Stage 38 Display Consumer Milestone Freeze

> 状态：**Stage 36 design、Stage 37 implementation、Stage 38 milestone freeze 均已收口**
> 当前稳定里程碑：`v0.16.0-filtered-snapshot-display-consumer`（本地 annotated tag，未 push）
> 上一里程碑：`v0.15.0-filtered-snapshot-display-integration` / `b1fa0b3`（已推送）
> 下一阶段：Stage 39 — Filtered Snapshot Markdown Display Consumer Host Integration Gate（条件启动）

## 1. 本轮完成

- Stage 36 本地提交 `342d0ae` 冻结标准库-only、stdin-only display v1 consumer contract；
- Stage 37 按 TDD 新增 `tools/codex_desktop_filtered_snapshot_display_consumer.py` 与 16 项专用测试；
- ready 时独立重算 content SHA-256，验证固定 Markdown grammar、安全 ASCII JSON literal、identity/count/filter/empty-view/report coherence；
- non-ready 只接受 withheld contract，输出不复制 content 或上游 finding message；
- 124 项跨阶段回归与 884 项全量测试通过；doctor、public scan、py_compile 和真实 ready/blocked stdio 管道通过；
- Stage 38 更新权威文档、release notes、CLI usage、versioning、README 与恢复上下文，并冻结本地 v0.16.0 annotated tag。

权威事实源：

- `docs/92-filtered-snapshot-markdown-display-consumer-validation-gate.md`
- `docs/archive/release-notes/98-release-notes-stage37-filtered-snapshot-markdown-display-consumer.md`
- `docs/archive/release-notes/99-release-notes-v0.16.0-filtered-snapshot-display-consumer.md`

## 2. 固定实现边界

- consumer 只从 stdin 读取一份最大 64 KiB 的完整 display v1 JSON；
- 不接受参数、file/path/URL/payload-only/raw Markdown；
- 不启动 display/host/reader/consumer/command/adapter；
- 不输出或持久化 content，不访问网络、不启动 service、不写入；
- 固定 contract/check/status/exit 见 `docs/92`。

## 3. Stage 39 条件启动边界

第一拍只允许 design gate，固定候选链路：

```text
Stage 34 display stdout
  -> Stage 37 consumer stdin
  -> validation-before-release one-shot host result
```

必须先冻结 argv ownership、bounded stdio、timeout/cancel/no-retry、identity cross-check、consumer pass 前 content withheld、failure mapping、memory cleanup 与 no-side-effect。不得直接实现专有 Codex Desktop UI、HTML/browser、file/URL、live service、network/DB/auth、cache/export、write 或真实 adapter execution。

## 4. Git 状态

- Stage 36 commit：`342d0ae`；
- Stage 38 freeze commit：以本 handoff 所在 commit 为准；
- local tag：`v0.16.0-filtered-snapshot-display-consumer`；
- 本轮不 push，代理关闭，不修改任何持久 Git/网络代理配置。
