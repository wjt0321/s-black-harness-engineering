# Handoff — 2026-07-16 — Stage 33 Filtered Snapshot Display Integration Gate

> 状态：**Stage 33 design gate 已收口；Stage 34 可按 TDD 条件启动**
> 当前稳定里程碑：`v0.14.0-filtered-snapshot-host-integration` / `dfae346`（已推送）
> 候选下一里程碑：`v0.15.0-filtered-snapshot-display-integration`

## 1. 本轮完成

- 审计具体展示面，选择 Codex Desktop 可消费的 deterministic escaped Markdown；
- 冻结 fixed Stage 31 host-owned wrapper，拒绝 arbitrary stdin presenter 与未冻结的专有插件 API；
- 冻结 host result gate、安全字段 allowlist、Markdown escaping、empty-view UX、状态/退出码与资源上限；
- 将 v0.14.0 历史冻结文档移入 `docs/archive/90-codex-desktop-filtered-snapshot-host-integration-and-milestone-freeze.md`；
- 确认 v0.14.0 tag 与 `main` 已在 `origin`，临时代理未写入 Git 配置。

权威事实源：

- `docs/91-codex-desktop-filtered-snapshot-markdown-display-integration-and-milestone-freeze.md`
- `docs/archive/release-notes/94-release-notes-stage33-filtered-snapshot-display-integration-gate.md`
- `docs/archive/90-codex-desktop-filtered-snapshot-host-integration-and-milestone-freeze.md`

## 2. Stage 34 固定实现边界

候选工具：

```text
tools/codex_desktop_filtered_snapshot_display.py
```

固定 contract：

```text
control-plane/codex-desktop-filtered-snapshot-display/v1
codex-desktop-filtered-snapshot-markdown-display/v1
```

固定数据流：

```text
explicit root/envelope/exact filters
  -> fixed Stage 31 host --json
  -> strict UTF-8 / duplicate-key / exact-shape validation
  -> safe allowlist projection
  -> escaped deterministic Markdown
  -> versioned display JSON
```

不得直接启动 Stage 27 reader 或 Stage 29 consumer，不接受 arbitrary stdin/file/URL，不执行 descriptor argv、candidate command 或 adapter。

## 3. 必须先 RED 的验收矩阵

- fixed host argv/cwd/stdin none/minimal env；
- request-only、task-only、AND 与合法空视图；
- deterministic content/content id；
- Markdown/HTML/link/backtick/pipe/control-character injection escaping；
- blocked/validation_failed/error mapping 与 content withheld；
- malformed/non-UTF8/duplicate/unknown/status-exit drift；
- identity、row allowlist/type/count/matched mismatch；
- timeout/stdout/stderr/final-size bounds 与 no retry；
- invalid root/filter/representation no spawn；
- real Stage 31 host → display smoke；
- Stage 18/20/22/24/27/29/31 compatibility regression。

## 4. 冻结停止线

Stage 34 通过后进入 Stage 35：

- 更新 doc91 为实现/验收/里程碑事实源；
- 新增 Stage 34/35 release notes；
- 全量 pytest、doctor、public scan、Git Bash hook、`git diff --check`；
- 创建本地 annotated tag `v0.15.0-filtered-snapshot-display-integration`；
- 未获当轮明确授权时不 push。

## 5. 继续 unavailable

专有插件/UI、HTML/browser、raw Markdown input、network/service/DB/auth、cache/export/clipboard/file、query/sort/page/lineage、UI write、approval resolve、candidate command 与真实 adapter execution。
