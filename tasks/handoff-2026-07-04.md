# 2026-07-04 接续上下文

> 本文件供压缩后恢复 `s-black harness engineering` 项目使用。当前项目根目录：仓库 checkout 根目录。GitHub 仓库：`https://github.com/wjt0321/s-black-harness-engineering`。

## 当前结论

`s-black harness engineering` 已完成并冻结 **Runtime read-only planning bridge** 阶段。

当前阶段 tag：

```text
v0.3.0-runtime-planning-bridge
```

当前 HEAD：

```text
e566d0b Add runtime draft validation and inspect
```

当前本地与远端应保持一致：

```text
main...origin/main
```

本阶段仍然是只读 POC：不执行真实 adapter，不访问网络，不发送消息，不删除文件，不写真实 task ledger / event ledger / adapter envelope，不读取 `.env` / credential。

## 当前完整链路

Runtime planning bridge 当前闭环为：

```text
task ledger
  -> runtime plan --draft-json
  -> runtime draft validate
  -> runtime draft inspect
  -> adapter validate / adapter inspect
  -> runtime gate check
  -> runtime check-ledger
```

各环节职责：

- `runtime plan`：给定 task + adapter action，生成 compact 草案摘要。
- `runtime plan --draft-json`：输出 schema-valid 的 envelope draft 到 stdout。
- `runtime draft validate`：校验 direct envelope 或 `runtime plan --draft-json` wrapper。
- `runtime draft inspect`：输出 draft 摘要，不回显完整 target / input / evidence / raw_ref / decision_ref。
- `adapter validate` / `adapter inspect`：复用 adapter envelope schema 与 consistency 规则。
- `runtime gate check`：聚合 task 状态与 adapter approval/response gate。
- `runtime check-ledger`：审计 task/event ledger 与 adapter envelope 的跨系统一致性。

## 当前已实现能力

### 基础 POC 与规则模型

- policy / task / event / agent / adapter schema 与样例。
- 只读 CLI POC。
- `doctor` 项目结构与样例校验。
- `check text` / `check path` / `check action`。
- agent -> policy profile registry 映射。
- public scan 产品化并纳入 CI。

### Task Ledger

- `task status`
- `task events`
- `task validate`
- `task check-ledger`

### Adapter Envelope

- `adapter plan`
- `adapter validate`
- `adapter inspect`
- `adapter approval check`
- `adapter response check`
- `adapter gate check`

### Runtime Bridge

- `runtime plan`
- `runtime plan --draft-json`
- `runtime draft validate`
- `runtime draft inspect`
- `runtime gate check`
- `runtime check-ledger`

## 关键文档

- `docs/13-release-notes-adapter-envelope.md` — v0.2 adapter envelope 阶段收口。
- `docs/archive/14-task-runtime-bridge.md` — task runtime bridge 设计。
- `docs/15-runtime-ledger-audit.md` — runtime ledger audit 设计。
- `docs/16-runtime-plan.md` — runtime plan 设计。
- `docs/17-runtime-planning-bridge.md` — v0.3 runtime planning bridge 闭环说明。
- `docs/18-release-notes-runtime-planning-bridge.md` — v0.3 release notes。

## 常用恢复命令

```bash
git status -sb
git log -1 --oneline
git tag --points-at HEAD
python -m pytest
python -m agent_runtime.cli doctor
python tools/public_scan.py
```

预期：

```text
main...origin/main
HEAD: e566d0b Add runtime draft validation and inspect
tag: v0.3.0-runtime-planning-bridge
python -m pytest -> 207 passed
python -m agent_runtime.cli doctor -> PASS
python tools/public_scan.py -> OK public scan
```

## 当前 CLI 抽查命令

```bash
python -m agent_runtime.cli runtime plan --task-id task-20260703-001 --adapter shell-local --operation read_file --target docs/06-adapter-layer.md
python -m agent_runtime.cli runtime plan --task-id task-20260703-001 --adapter github-cli --operation git_push --target origin/main --draft-json
python -m agent_runtime.cli runtime draft validate --file adapters/execution-envelope.examples.json
python -m agent_runtime.cli runtime draft inspect --file adapters/execution-envelope.examples.json
python -m agent_runtime.cli runtime gate check --task-id task-20260703-001 --request-id req-20260703-002 --envelope adapters/execution-envelope.examples.json
python -m agent_runtime.cli runtime check-ledger --tasks-file tasks/tasks.jsonl --events-file tasks/events.jsonl --envelope adapters/execution-envelope.examples.json
```

注意：仓库样例里的 `task-20260703-001` 已经是 `finished`，所以 `runtime plan` 与 `runtime gate check` 在该 task 上返回 `BLOCKED` 是预期行为。

## 最新验证结果

v0.3 阶段收口前验证：

```text
python -m pytest -> 207 passed
python -m agent_runtime.cli doctor -> PASS
python tools/public_scan.py -> OK public scan
git diff --check -> PASS
```

命令抽查：

```text
runtime draft validate --file adapters/execution-envelope.examples.json -> PASS
runtime draft inspect --file adapters/execution-envelope.examples.json -> PASS
```

## 安全边界

当前仍保持只读：

- 不执行真实 adapter。
- 不访问网络。
- 不发送消息。
- 不删除文件。
- 不写 `tasks/tasks.jsonl`、`tasks/events.jsonl` 或 adapter envelope 文件。
- 不读取 `.env`、`.env.local` 或 credential 文件。
- 不回显完整 secret match。
- 不回显完整 `input`、`evidence`、`raw_ref`、`decision_ref`。
- pending `approval_record` 不再强制包含 `decision_ref` 字段；历史样例若带 `decision_ref: null` 仍兼容。

## 推送记录与注意事项

最近一次 v0.3 main/tag 推送时 GitHub 直连超时，处理流程如下：

1. 先跑 `python -m pytest`、`python -m agent_runtime.cli doctor`、`python tools/public_scan.py`。
2. 直连失败后，临时启用维护者工具工作区里的 V2Ray 启动脚本。
3. 在项目 checkout 根目录注入：

```bat
set HTTP_PROXY=http://127.0.0.1:10808&& set HTTPS_PROXY=http://127.0.0.1:10808&& git push
```

4. 推送完成后执行维护者工具工作区的关闭脚本，并确认系统代理恢复。

本轮 v0.3 推送完成后已关闭 V2Ray，系统代理已恢复。

## 下一阶段建议

下一阶段不要急着进入真实写入。建议二选一：

### 方向 A：继续只读聚合

新增 `runtime report` / `runtime handoff`：一条命令汇总当前 task、draft、gate、ledger audit 状态，输出面向人和机器的报告。

可能命令：

```bash
python -m agent_runtime.cli runtime report --task-id ... --envelope ...
```

目标：让 orchestrator 快速判断“这个任务现在处于什么阶段、缺什么、下一步是什么”。

### 方向 B：设计受控写入前置规范

先写设计，不实现写入：

- draft export 的授权规则。
- event append 的授权规则。
- 写入前 schema validation。
- 写入后 ledger consistency validation。
- rollback / recovery 约束。

可能文档：

```text
docs/19-controlled-write-boundaries.md
```

建议优先方向 A。理由：继续保持只读，同时把已有能力组合成更有用的交接报告，风险低，收益高。

## 分工提醒

代码实现仍优先交给 Kimi Code；主控负责：

- 任务边界
- 安全约束
- 代码审查
- 本地验证
- public scan
- commit / push / tag
- 对用户汇报

## 当前推荐下一步

优先做：

```text
runtime report / runtime handoff 只读聚合报告
```

不要直接做真实写入。真实 draft export / ledger append 应等授权边界文档明确后再实现。
