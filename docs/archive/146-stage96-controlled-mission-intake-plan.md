# 146 — 阶段 96 实施计划：受控任务登记与规划收件箱

> 状态：已完成并归档
> 日期：2026-07-29

1. **输入与账本（测试先行）**：为 `agent_deck_mission_intake` 编写预览、提交、自动编号和 secret scan 测试；实现固定候选任务并委托既有 `submit_task`。
2. **CLI（测试先行）**：加入 `agent-deck mission submit`，仅暴露 `--goal` 与互斥的 `--dry-run/--commit`，确保 JSON 不含自由目标文本。
3. **看板投影（测试先行）**：让 `planned` 任务可展示，并仅为严格来源的 Deck 任务显示固定等待规划标签。
4. **前端（测试先行）**：扩展 task 类型与任务卡，展示等待规划状态且继续断言不存在任何启动入口。
5. **交付**：执行真实 dry-run、全量 Python/前端回归和治理检查；归档设计/计划/验收文档，更新入口索引并推送。
