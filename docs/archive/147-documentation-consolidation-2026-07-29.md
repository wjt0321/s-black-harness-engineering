# 147 — 文档收敛与阶段 97 恢复包

> 状态：已完成并归档
> 日期：2026-07-29
> 范围：活跃 `docs/`、CLI 入口、恢复 handoff 与产品阶段上下文；不删除历史事实。

## 1. 盘点与价值判断

本次盘点了 28 份活跃 Markdown 文档、归档目录和 `tasks/handoff-*.md`。处理原则是：当前入口保持短小、当前架构保留在根目录、阶段事实留在 archive、历史过程不删除而是移入可追溯归档。

| 范围 | 处理 | 理由 |
|:---|:---|:---|
| `README.md`、`000-stage-digest.md`、`00-index.md`、`02-roadmap.md` | 保留并更新 | 分别是用户入口、恢复断点、导航和下一能力包权威入口。 |
| `01`–`08`、`12`、`15`–`21`、`47`–`52`、`64` | 保留 | 仍承载架构、策略、账本、编排与治理的独立事实，不与阶段验收文档重复。 |
| `111`、`113`、`130` | 保留并校正当前上下文 | 分别为固定 Pi、运行时 binding 和 Agent Deck 长期目标的直接事实源。 |
| `10-cli-poc-usage.md`（2,966 行）与 `64-versioning-governance.md`（452 行） | 原样归档，根目录替换为当前短入口 | 两份旧文件混合历史阶段过程、过期结论和当前规则；保留原文供追溯，避免继续作为日常操作手册。 |
| `docs/archive/` | 保留 | 已完成设计、计划、验收和旧入口均可由 Git 与 archive 追溯。 |
| `tasks/handoff-*.md` | 保留历史，新建当前 handoff | 历史恢复记录不可删除；当前会话只应读取最新的阶段 97 恢复包。 |

## 2. 收敛结果

- 当前 CLI 与版本治理入口由 3,418 行历史记录收敛为聚焦恢复、诊断、Agent Deck 任务登记、真实执行边界、发布纪律与验证的短手册；
- 原完整内容以 `archive/10-cli-poc-usage-legacy-2026-07-29.md` 和 `archive/64-versioning-governance-legacy-2026-07-29.md` 原样保留，没有信息删除；
- 路线图移除已归档阶段的重复叙述，改为能力包、阶段 94–96 当前主线和阶段 97 候选；
- 长期产品目标更新到阶段 96，明确任务登记已完成但结构化主控规划尚未实施；
- 新的 `tasks/handoff-2026-07-29-stage97.md` 记录下阶段的唯一启动上下文、设计问题、必须复用的能力和禁止项；
- `AGENTS.md`、digest、index、README 均指向最新恢复入口。

## 3. 保留的下一阶段边界

阶段 97 的目标是“受限主控 Agent 结构化规划提议”，不是直接让网页或任务草案执行 Agent。它必须独立冻结：固定输入、结构化输出 schema、敏感信息扫描、存储与审计、人与现有 Pi→OMP→Pi 链路之间的确认门。它不得扩展通用 shell、宿主生命周期、任意工具、网络、并发、重试或自治执行。

## 4. 验证

- 已运行 docs context，确认阶段 96 与阶段 97 恢复入口可解析；
- 已运行文档维护钩子、公开扫描、链接/差异检查和全量测试；
- 已确认当前活跃文档链接均有效；归档 snapshot 保持其历史路径快照，不作为当前链接检查或操作入口。
