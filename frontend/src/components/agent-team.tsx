import type {DeckAgent} from "@/lib/agent-deck-types";import {Bot,WifiOff} from "lucide-react";export function AgentTeam({agents}:{agents:DeckAgent[]}){return <section><div className="mb-3 flex items-center justify-between"><h2 className="text-lg font-semibold">Agent 团队</h2><span className="text-sm text-zinc-500">首批试运行成员</span></div><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{agents.map(agent=><article key={agent.id} className="rounded-xl border border-zinc-800 bg-zinc-900 p-4"><div className="flex items-start justify-between"><div><Bot className="mb-2 size-5 text-amber-300"/><h3 className="font-medium">{agent.name_zh}</h3><p className="text-sm text-zinc-400">{agent.role_zh}</p></div><span className="rounded-full bg-zinc-800 px-2 py-1 text-xs">{agent.status_label_zh}</span></div><p className="mt-4 text-sm text-zinc-300">{agent.safe_summary_zh}</p>{agent.integration_status==='not_integrated'&&<p className="mt-3 text-xs text-zinc-500"><WifiOff className="mr-1 inline size-3"/>尚未接入真实状态或执行能力</p>}</article>)}</div></section>}
