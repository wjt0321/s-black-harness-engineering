import type {AgentDeckSnapshot} from "@/lib/agent-deck-types";export function CollaborationTimeline({snapshot}:{snapshot:AgentDeckSnapshot}){return <section className="rounded-xl border border-zinc-800 bg-zinc-900 p-4"><h2 className="text-lg font-semibold">协作时间线</h2>{snapshot.timeline.length===0?<p className="mt-3 text-sm text-zinc-400">暂无正在展示的安全协作事件。</p>:<ol className="mt-3 space-y-3">{snapshot.timeline.map(item=><li key={item.chain_id} className="border-l-2 border-amber-400 pl-3"><p className="font-medium">{item.status_label_zh||item.status}</p><p className="text-sm text-zinc-400">{item.safe_summary_zh||"已记录安全状态。"}</p></li>)}</ol>}<details className="mt-4 text-sm text-zinc-400"><summary>过程与证据</summary><p className="mt-2">只显示受控链路的安全摘要；原始输出和内部审计按需在既有控制面查看。</p></details></section>}
