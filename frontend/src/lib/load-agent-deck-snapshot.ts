import {type AgentDeckSnapshot,isAgentDeckSnapshot} from "./agent-deck-types";export type RuntimeState={kind:"loading"}|{kind:"live";snapshot:AgentDeckSnapshot}|{kind:"unavailable";message:string}|{kind:"invalid";message:string};export async function loadAgentDeckSnapshot():Promise<RuntimeState>{try{const response=await fetch("/agent-deck.snapshot.json",{cache:"no-store"});if(!response.ok)return{kind:"unavailable",message:"尚未生成 Agent Deck 安全快照。"};const value:unknown=await response.json();return isAgentDeckSnapshot(value)?{kind:"live",snapshot:value}:{kind:"invalid",message:"Agent Deck 快照格式无效，已拒绝展示。"}}catch{return{kind:"unavailable",message:"无法读取实时 Agent Deck 快照。"}}}
